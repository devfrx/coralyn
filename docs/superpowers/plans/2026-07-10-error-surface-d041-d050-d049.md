# Error-surface slice (D-041 + D-050 + D-049) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Chiudere il capitolo error-surface §4: mappare uniformemente gli errori Prisma raw che oggi sfuggono come **500** verso status HTTP puliti (`P2002`→409 [D-041], `P2023`→400 [D-050]) tramite un unico filtro globale, e rendere il full-run e2e robusto senza flag (`testTimeout` [D-049]).

**Architecture:** Un `PrismaExceptionFilter` (`@Catch(Prisma.PrismaClientKnownRequestError)`, estende `BaseExceptionFilter`) mappa i codici noti e **delega i non gestiti a `super.catch`** (500 + log invariati). Registrato via **`APP_FILTER`** in `AppModule` → attivo in prod **e in tutte le e2e per costruzione** (le suite importano `AppModule`), zero drift test-vs-prod. Le difese locali (`ParseUUIDPipe` struttura §4.1, `mapConflict`/check-proattivi 409) **restano** come prima linea con messaggi specifici; il filtro è il backstop uniforme. In parallelo si estrae `createTestApp()` per eliminare la duplicazione (22×) del bootstrap `prefix + ValidationPipe` nelle e2e.

**Tech Stack:** NestJS 10, Prisma, Jest + supertest (e2e), pnpm (corepack).

## Global Constraints

- **Package manager:** `corepack pnpm` — MAI `npm install`. Comandi dai rispettivi workspace (`apps/api`).
- **e2e:** SEMPRE `--runInBand`. Full-run: `pnpm --filter @coralyn/api test:e2e` (dopo Task 1 non serve più `--testTimeout`). Mirati: `--config ./test/jest-e2e.json -t '<nome>'` (senza `--config` matchano 0 test = falso pass).
- **Baseline da NON regredire:** api unit **229** · api e2e **317** · web-staff **375** (non toccato) · typecheck api `tsc` pulito. FE non toccato (nessun `vue-tsc`).
- **NON in parallelo:** `@coralyn/web-staff test` e `@coralyn/api test:e2e` (contendono `prepare`/`build:contracts`).
- **DoD [ADR-0009]:** nessun cambio a modello/flusso/macchina-a-stati/UI → **nessun design doc, nessun nuovo ADR**. Si aggiorna solo `docs/architecture/deferred.md`.
- **Messaggi:** italiano, coerenti con lo stile esistente.
- **UUID semantics:** `ParseUUIDPipe`/`@IsUUID()` default = v1–5; gli id Prisma sono v4 → passano sempre. NON allentare a "any".

---

### Task 1: D-049 — `testTimeout` esplicito nella config e2e

**Files:**
- Modify: `apps/api/test/jest-e2e.json`

**Interfaces:**
- Consumes: nulla.
- Produces: full-run e2e stabile senza `--testTimeout` (usato dalla verifica di tutti i task successivi).

- [ ] **Step 1: Aggiungere `testTimeout` alla config**

In `apps/api/test/jest-e2e.json`, aggiungere la chiave `"testTimeout": 20000` (i tempi reali di init sono < 5s a macchina scarica → non maschera regressioni di performance):

```json
{
  "moduleFileExtensions": ["js", "json", "ts"],
  "rootDir": ".",
  "testEnvironment": "node",
  "testRegex": ".e2e-spec.ts$",
  "transform": { "^.+\\.ts$": "ts-jest" },
  "setupFiles": ["./jest-setup-env.ts"],
  "testTimeout": 20000
}
```

- [ ] **Step 2: Verificare il full-run SENZA il flag manuale**

Run (da `apps/api`): `corepack pnpm test:e2e -- --runInBand`
Expected: **317 passed**, nessun «Exceeded timeout of 5000 ms for a hook».

- [ ] **Step 3: Commit**

```bash
git add apps/api/test/jest-e2e.json
git commit -m "test(api): testTimeout 20s esplicito nella config e2e (D-049)"
```

---

### Task 2: `PrismaExceptionFilter` + mapper puro + registrazione `APP_FILTER` (D-041 + D-050)

**Files:**
- Create: `apps/api/src/common/prisma-exception.filter.ts`
- Create: `apps/api/src/common/prisma-exception.filter.spec.ts`
- Modify: `apps/api/src/app.module.ts`

**Interfaces:**
- Consumes: `Prisma.PrismaClientKnownRequestError` (da `@prisma/client`), `BaseExceptionFilter` (da `@nestjs/core`).
- Produces:
  - `mapPrismaKnownError(code: string): { status: number; message: string } | null` — funzione pura, `null` = delega al default.
  - `class PrismaExceptionFilter extends BaseExceptionFilter` — registrato via `APP_FILTER`.

- [ ] **Step 1: Scrivere i test del mapper puro (falliscono)**

Create `apps/api/src/common/prisma-exception.filter.spec.ts`:

```ts
import { HttpStatus } from '@nestjs/common';
import { mapPrismaKnownError } from './prisma-exception.filter';

describe('mapPrismaKnownError', () => {
  it('mappa P2002 → 409 con messaggio di conflitto', () => {
    const m = mapPrismaKnownError('P2002');
    expect(m).toEqual({
      status: HttpStatus.CONFLICT,
      message: 'Operazione in conflitto: esiste già una risorsa con questi dati.',
    });
  });

  it('mappa P2023 → 400 con messaggio di id non valido', () => {
    const m = mapPrismaKnownError('P2023');
    expect(m).toEqual({
      status: HttpStatus.BAD_REQUEST,
      message: 'Identificatore non valido.',
    });
  });

  it('restituisce null (delega al default) per i codici non gestiti', () => {
    expect(mapPrismaKnownError('P2003')).toBeNull(); // FK
    expect(mapPrismaKnownError('P2025')).toBeNull(); // record not found
    expect(mapPrismaKnownError('P2034')).toBeNull(); // deadlock/write conflict
    expect(mapPrismaKnownError('P1000')).toBeNull();
  });
});
```

- [ ] **Step 2: Eseguire il test → deve fallire**

Run (da `apps/api`): `corepack pnpm test -- prisma-exception.filter`
Expected: FAIL — «Cannot find module './prisma-exception.filter'».

- [ ] **Step 3: Implementare il filtro e il mapper puro**

Create `apps/api/src/common/prisma-exception.filter.ts`:

```ts
import { ArgumentsHost, Catch, HttpStatus } from '@nestjs/common';
import { BaseExceptionFilter } from '@nestjs/core';
import { Prisma } from '@prisma/client';
import type { Response } from 'express';

/**
 * Mappa i codici Prisma noti che altrimenti sfuggirebbero come 500 verso status HTTP puliti.
 * `null` = codice non gestito → il chiamante delega al comportamento di default (500 + log).
 * D-041 (P2002→409) e D-050 (P2023→400): companion, gestiti in un unico punto.
 */
export function mapPrismaKnownError(code: string): { status: number; message: string } | null {
  switch (code) {
    case 'P2002':
      return {
        status: HttpStatus.CONFLICT,
        message: 'Operazione in conflitto: esiste già una risorsa con questi dati.',
      };
    case 'P2023':
      return {
        status: HttpStatus.BAD_REQUEST,
        message: 'Identificatore non valido.',
      };
    default:
      return null;
  }
}

@Catch(Prisma.PrismaClientKnownRequestError)
export class PrismaExceptionFilter extends BaseExceptionFilter {
  catch(exception: Prisma.PrismaClientKnownRequestError, host: ArgumentsHost): void {
    const mapped = mapPrismaKnownError(exception.code);
    if (!mapped) {
      // Codici non gestiti (P2003/P2025/P2034/…): comportamento INVARIATO (default Nest = 500 + log).
      super.catch(exception, host);
      return;
    }
    const res = host.switchToHttp().getResponse<Response>();
    res.status(mapped.status).json({
      statusCode: mapped.status,
      message: mapped.message,
      error: mapped.status === HttpStatus.CONFLICT ? 'Conflict' : 'Bad Request',
    });
  }
}
```

- [ ] **Step 4: Registrare il filtro via `APP_FILTER` in `AppModule`**

Modify `apps/api/src/app.module.ts` — aggiungere l'import e il blocco `providers`:

```ts
import { Module } from '@nestjs/common';
import { APP_FILTER } from '@nestjs/core';
import { ConfigModule } from '@nestjs/config';
import { HealthController } from './health/health.controller';
import { PrismaModule } from './prisma/prisma.module';
import { TenantModule } from './tenant/tenant.module';
import { CustomersModule } from './customers/customers.module';
import { MapModule } from './map/map.module';
import { BookingsModule } from './bookings/bookings.module';
import { IdentityModule } from './identity/identity.module';
import { CatalogModule } from './catalog/catalog.module';
import { ReportsModule } from './reports/reports.module';
import { EstablishmentModule } from './establishment/establishment.module';
import { PlatformModule } from './platform/platform.module';
import { PrismaExceptionFilter } from './common/prisma-exception.filter';

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    PrismaModule,
    TenantModule,
    IdentityModule,
    CustomersModule,
    MapModule,
    CatalogModule,
    BookingsModule,
    ReportsModule,
    EstablishmentModule,
    PlatformModule,
  ],
  controllers: [HealthController],
  providers: [{ provide: APP_FILTER, useClass: PrismaExceptionFilter }],
})
export class AppModule {}
```

- [ ] **Step 5: Eseguire i test unit → devono passare**

Run (da `apps/api`): `corepack pnpm test -- prisma-exception.filter`
Expected: **3 passed**.

- [ ] **Step 6: Typecheck**

Run (da `apps/api`): `corepack pnpm typecheck` (o `tsc -p tsconfig.json --noEmit` come da script del repo)
Expected: 0 errori.

- [ ] **Step 7: Commit**

```bash
git add apps/api/src/common/prisma-exception.filter.ts apps/api/src/common/prisma-exception.filter.spec.ts apps/api/src/app.module.ts
git commit -m "feat(api): PrismaExceptionFilter globale (P2002->409 D-041, P2023->400 D-050) via APP_FILTER"
```

---

### Task 3: Estrarre `createTestApp()` ed eliminare la duplicazione del bootstrap e2e

**Files:**
- Create: `apps/api/test/helpers/create-test-app.ts`
- Modify: le e2e-spec che bootstrappano l'app (le 22 con `useGlobalPipes`; le 3 senza vanno valutate — vedi Step 4).

**Interfaces:**
- Consumes: `TestingModule` compilato dalla suite (`Test.createTestingModule({...}).compile()`).
- Produces: `createTestApp(moduleRef: TestingModule): Promise<INestApplication>` — applica prefix `api` + `ValidationPipe` + `init()` e ritorna l'app. **NON** applica il filtro (già globale via `APP_FILTER` del Task 2).

- [ ] **Step 1: Creare l'helper**

Create `apps/api/test/helpers/create-test-app.ts`:

```ts
import { INestApplication, ValidationPipe } from '@nestjs/common';
import type { TestingModule } from '@nestjs/testing';

/**
 * Bootstrap e2e allineato a main.ts: prefix `api` + ValidationPipe(whitelist,transform) + init.
 * Il PrismaExceptionFilter NON va applicato qui: è registrato via APP_FILTER in AppModule, quindi
 * è già attivo in ogni test che importa AppModule (zero drift test-vs-prod per costruzione).
 */
export async function createTestApp(moduleRef: TestingModule): Promise<INestApplication> {
  const app = moduleRef.createNestApplication();
  app.setGlobalPrefix('api', { exclude: ['health'] });
  app.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true }));
  await app.init();
  return app;
}
```

- [ ] **Step 2: Migrare UNA suite come pilota (`equipment-types.e2e-spec.ts`)**

Nel `beforeAll`, sostituire il blocco:

```ts
    app = moduleRef.createNestApplication();
    app.setGlobalPrefix('api', { exclude: ['health'] });
    app.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true }));
    await app.init();
```

con:

```ts
    app = await createTestApp(moduleRef);
```

Aggiungere l'import `import { createTestApp } from './helpers/create-test-app';` e **rimuovere** l'import ora inutilizzato `ValidationPipe` (lasciando `INestApplication`). Rieseguire la suite pilota:

Run (da `apps/api`): `corepack pnpm test:e2e -- --runInBand --config ./test/jest-e2e.json -t 'EquipmentTypes'`
Expected: PASS (stesso conteggio della suite).

- [ ] **Step 3: Migrare le restanti 21 suite con `useGlobalPipes`**

Per ciascuna suite che contiene `app.useGlobalPipes(new ValidationPipe(...))` nel bootstrap, applicare la stessa sostituzione dello Step 2 (blocco `createNestApplication`+prefix+pipe+init → `app = await createTestApp(moduleRef);`), aggiungere l'import dell'helper, rimuovere l'import `ValidationPipe` se non più usato. **NB:** alcune suite hanno più `beforeAll`/blocchi (es. `customers.e2e-spec.ts` ne ha due) → migrarli tutti.

- [ ] **Step 4: Valutare le 3 suite SENZA `useGlobalPipes`**

Identificarle: `grep -L useGlobalPipes apps/api/test/*.e2e-spec.ts`. Per ciascuna, decidere:
- Se il bootstrap è `createNestApplication` + prefix + `init` (senza pipe) e la suite **non dipende** dall'assenza della pipe (nessun test invia campi extra che `whitelist` scarterebbe, né si affida a tipi non trasformati) → migrare a `createTestApp` (aggiunge pipe+coerenza; la pipe è additiva e sicura). Rieseguire la suite: se verde, tenere.
- Se la migrazione rompe la suite (la pipe cambia il comportamento atteso) → **NON migrare**; lasciare il bootstrap manuale con un commento `// bootstrap senza ValidationPipe: <motivo>` così l'incoerenza è tracciata, non silenziosa.
Riportare al gate di review quali delle 3 sono state migrate e quali no, con motivazione.

- [ ] **Step 5: Full-run e2e verde**

Run (da `apps/api`): `corepack pnpm test:e2e -- --runInBand`
Expected: **317 passed** (invariato — refactor puro di bootstrap).

- [ ] **Step 6: Typecheck**

Run (da `apps/api`): `corepack pnpm typecheck`
Expected: 0 errori (nessun import `ValidationPipe` orfano).

- [ ] **Step 7: Commit**

```bash
git add apps/api/test/
git commit -m "test(api): estrai createTestApp() ed elimina la duplicazione del bootstrap e2e"
```

---

### Task 4: e2e di prova D-050 — id malformato sui controller catalog → 400

**Files:**
- Create: `apps/api/test/catalog-error-surface.e2e-spec.ts`

**Interfaces:**
- Consumes: `createTestApp` (Task 3); il `PrismaExceptionFilter` è già attivo via `APP_FILTER`.
- Produces: copertura e2e end-to-end del path `P2023`→400 sul catalog (prima del filtro erano **500**).

- [ ] **Step 1: Scrivere la suite (fallirebbe senza il filtro — 500)**

Create `apps/api/test/catalog-error-surface.e2e-spec.ts`. Un solo tenant/admin; per ogni endpoint catalog con `:id` grezzo, un id malformato non-UUID (`'not-a-uuid'`) deve dare **400** (non 500). Coprire `equipment-types` (PATCH, archive, restore, DELETE), `packages` (idem), `rates` (PATCH, DELETE), `seasons` (DELETE), `time-slots` (PATCH, DELETE):

```ts
import { INestApplication } from '@nestjs/common';
import { Test } from '@nestjs/testing';
import { Role } from '@prisma/client';
import request from 'supertest';
import { AppModule } from '../src/app.module';
import { PrismaService } from '../src/prisma/prisma.service';
import { createTestApp } from './helpers/create-test-app';
import { createUser, login } from './helpers/seed-auth';

describe('Catalog error-surface — id malformato → 400 (D-050)', () => {
  let app: INestApplication;
  let prisma: PrismaService;
  let sid: string;
  let token: string;
  const bearer = (t: string): [string, string] => ['Authorization', `Bearer ${t}`];
  const BAD = 'not-a-uuid';

  beforeAll(async () => {
    const moduleRef = await Test.createTestingModule({ imports: [AppModule] }).compile();
    app = await createTestApp(moduleRef);
    prisma = app.get(PrismaService);
    sid = (await prisma.establishment.create({ data: { name: 'Cat ErrSurface' } })).id;
    await createUser(prisma, {
      email: 'admin.caterr@e2e.test', password: 'pw', role: Role.admin, establishmentId: sid,
    });
    token = await login(app, 'admin.caterr@e2e.test', 'pw');
  });

  afterAll(async () => {
    await prisma.user.deleteMany({ where: { establishmentId: sid } });
    await prisma.establishment.delete({ where: { id: sid } });
    await app.close();
  });

  const cases: Array<[string, 'patch' | 'post' | 'delete', string, object | undefined]> = [
    ['equipment-types PATCH', 'patch', `/api/equipment-types/${BAD}`, { name: 'x' }],
    ['equipment-types archive', 'post', `/api/equipment-types/${BAD}/archive`, undefined],
    ['equipment-types restore', 'post', `/api/equipment-types/${BAD}/restore`, undefined],
    ['equipment-types DELETE', 'delete', `/api/equipment-types/${BAD}`, undefined],
    ['packages PATCH', 'patch', `/api/packages/${BAD}`, { name: 'x' }],
    ['packages archive', 'post', `/api/packages/${BAD}/archive`, undefined],
    ['packages restore', 'post', `/api/packages/${BAD}/restore`, undefined],
    ['packages DELETE', 'delete', `/api/packages/${BAD}`, undefined],
    ['rates PATCH', 'patch', `/api/rates/${BAD}`, { price: 10 }],
    ['rates DELETE', 'delete', `/api/rates/${BAD}`, undefined],
    ['seasons DELETE', 'delete', `/api/seasons/${BAD}`, undefined],
    ['time-slots PATCH', 'patch', `/api/time-slots/${BAD}`, { label: 'x' }],
    ['time-slots DELETE', 'delete', `/api/time-slots/${BAD}`, undefined],
  ];

  it.each(cases)('%s → 400', async (_name, method, url, body) => {
    const req = request(app.getHttpServer())[method](url).set(...bearer(token));
    const res = await (body ? req.send(body) : req);
    expect(res.status).toBe(400);
  });
});
```

**NB:** verificare i nomi/forma dei body DTO reali (`UpdateEquipmentTypeDto`, `UpdatePackageDto`, `UpdateRateDto`, `UpdateTimeSlotDto`) leggendo i rispettivi file `dto/`; se un campo minimo valido è richiesto, usarlo — l'obiettivo è che il **path id-malformato** produca 400 *prima* che la validazione del body diventi rilevante (il `P2023` scatta al primo accesso Prisma con l'id). Se un endpoint valida il body PRIMA di toccare l'id e dà 400 per il body, va bene comunque (resta 400): ma preferire un body valido per isolare la causa all'id.

- [ ] **Step 2: Eseguire la suite → deve passare (col filtro attivo)**

Run (da `apps/api`): `corepack pnpm test:e2e -- --runInBand --config ./test/jest-e2e.json -t 'Catalog error-surface'`
Expected: **13 passed**. (Sanity: rimuovendo temporaneamente il `PRISMA_FILTER` da `AppModule` questi sarebbero 500 — non farlo nel commit, solo verifica mentale che il test è significativo.)

- [ ] **Step 3: Full-run e2e verde (nuovo totale)**

Run (da `apps/api`): `corepack pnpm test:e2e -- --runInBand`
Expected: **330 passed** (317 + 13). Registrare il numero reale.

- [ ] **Step 4: Commit**

```bash
git add apps/api/test/catalog-error-surface.e2e-spec.ts
git commit -m "test(api): e2e id malformato catalog -> 400 via PrismaExceptionFilter (D-050)"
```

---

### Task 5: Documentazione (deferred.md → Risolte) + verifica finale

**Files:**
- Modify: `docs/architecture/deferred.md`
- Create: `docs/handoff/2026-07-10-error-surface-slice-d041-d050-d049.md`

**Interfaces:**
- Consumes: gli esiti dei Task 1–4.
- Produces: registro deferred aggiornato + handoff.

- [ ] **Step 1: Spostare D-041, D-049, D-050 in «Risolte»**

In `docs/architecture/deferred.md`: rimuovere le righe D-041, D-049, D-050 dalla tabella e aggiungere in fondo, sezione «Risolte», tre voci con:
- **D-041 + D-050** → risolte insieme dal `PrismaExceptionFilter` globale (`P2002`→409, `P2023`→400) registrato via **`APP_FILTER`** (non `main.ts`), che estende `BaseExceptionFilter` e **delega i codici non gestiti** a `super.catch` (500 + log invariati); difese locali (`ParseUUIDPipe` struttura, `mapConflict`/check-proattivi) mantenute come prima linea. Nessun nuovo ADR (error-surface). File: `apps/api/src/common/prisma-exception.filter.ts`. Menzionare l'estrazione di `createTestApp()` (rimozione della duplicazione del bootstrap e2e).
- **D-049** → `"testTimeout": 20000` in `test/jest-e2e.json`; full-run stabile senza flag.
Includere i conteggi baseline finali.

- [ ] **Step 2: Verifica finale completa**

Run (da `apps/api`):
- `corepack pnpm test -- --runInBand` (unit) → Expected: **229 + N** (N = test del mapper, atteso 232).
- `corepack pnpm test:e2e -- --runInBand` (e2e) → Expected: **~330**.
- `corepack pnpm typecheck` → Expected: 0 errori.
Registrare i numeri reali nell'handoff.

- [ ] **Step 3: Verifica LIVE su Docker (metodo)**

Ricostruire il container api e verificare il path reale nel browser/via API:
```bash
docker compose --profile full up -d --build api
```
Poi (login `admin@coralyn.dev` / `coralyn-admin-8473`, o via curl con token) chiamare es. `DELETE /api/seasons/not-a-uuid` → **400** (prima: 500). Annotare l'esito nell'handoff.

- [ ] **Step 4: Scrivere l'handoff**

Create `docs/handoff/2026-07-10-error-surface-slice-d041-d050-d049.md`: stato git, cosa fatto (D-041/D-050/D-049 + createTestApp), scelte (APP_FILTER vs main.ts, BaseExceptionFilter super.catch, no nuovi ParseUUIDPipe catalog), baseline finale, gotcha, prossime priorità (D-035 S3/S4, D-040, backlog).

- [ ] **Step 5: Commit**

```bash
git add docs/architecture/deferred.md docs/handoff/2026-07-10-error-surface-slice-d041-d050-d049.md
git commit -m "docs: chiudi D-041/D-050/D-049 (error-surface) + handoff"
```

---

## Note di esecuzione (fuori dai commit)

- **Merge FF su `main` e push:** entrambi **gated** — solo dopo OK esplicito dell'utente, dopo la whole-branch review su opus (metodo).
- **Branch di lavoro:** creare `feat/error-surface-d041-d050-d049` da `main` (`abbabc1`) prima del Task 1.
- **Ambiente condiviso:** DB `coralyn_dev` + working tree condivisi tra sessioni → se lo stato cambia sotto, `db:reset -- --yes` + seed (con `DEV_ADMIN_PASSWORD=coralyn-admin-8473`) prima di debuggare.

[D-041]: ../../architecture/deferred.md
[D-049]: ../../architecture/deferred.md
[D-050]: ../../architecture/deferred.md
[ADR-0009]: ../../architecture/decisions/0009-documentazione-di-design.md
