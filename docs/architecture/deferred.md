# Decisioni rimandate (Deferred decisions)

Registro delle decisioni e dei compromessi **consapevolmente rimandati**. Una voce qui
significa: sappiamo che va affrontata, ma non ora.

Vedi [ADR-0002](decisions/0002-decision-rubric.md), filtro 4: il debito tracciato è ammesso,
quello silenzioso no.

**Come si usa.** Una voce vive in **`## Aperte`** finché non è affrontata; quando lo è, si
sposta in **`## Chiuse`** con il riferimento all’ADR, al branch o al commit che la chiude — non
si cancella, perché la ragione per cui una cosa è stata rimandata resta utile anche dopo. Ogni
voce compare **una volta sola** e ha un anchor stabile (`#d-0NN`), così un documento può citarla
con precisione.

⚠️ **Le tre regole qui sopra sono verificate da un test**, non affidate alla buona volontà:
[`deferred-registry.spec.ts`](../../packages/docs-lint/src/deferred-registry.spec.ts) diventa
rosso se un ID compare due volte, se una voce sta nella sezione sbagliata rispetto a ciò che
dichiara di sé, o se l’indice qui sotto non combacia con le voci. Il registro era arrivato ad
avere **6 voci chiuse fra le aperte, 4 aperte fra le chiuse e 2 duplicate** senza che nulla lo
segnalasse: vedi [ADR-0059](decisions/0059-gate-link-documenti.md) per la stessa medicina
applicata ai link.

## Indice

**Aperte: 42** · **Chiuse: 26** · totale 68.

| ID | Tema | Stato |
|---|---|---|
| [D-001](#d-001) | Stack tecnologico | ✅ chiusa |
| [D-002](#d-002) | Infrastruttura multi-tenancy completa (signup self-service, billing) | 🔓 aperta |
| [D-003](#d-003) | Internazionalizzazione della UI (i18n) | 🔓 aperta |
| [D-004](#d-004) | Pagamenti online, POS e scontrino elettronico | 🔓 aperta |
| [D-005](#d-005) | Editor planimetria libero della mappa | 🔓 aperta |
| [D-006](#d-006) | Liste d'attesa avanzate con hold a scadenza | 🔓 aperta |
| [D-007](#d-007) | Wrapper desktop Electron | 🔓 aperta |
| [D-008](#d-008) | Offline-sync completo della PWA | 🔓 aperta |
| [D-009](#d-009) | Entità `Pagamento` completa (acconti, ricevute, storni) | 🔓 aperta |
| [D-010](#d-010) | Isolamento fisico per tenant grandi (DB dedicato) | 🔓 aperta |
| [D-011](#d-011) | Prelazione abbonamenti | ✅ chiusa |
| [D-012](#d-012) | Cabine e servizi accessori come risorse prenotabili | 🔓 aperta |
| [D-013](#d-013) | Sospensione, cessione e disdetta dell'abbonamento | ✅ chiusa |
| [D-014](#d-014) | Gestione personale e turni bagnini | 🔓 aperta |
| [D-015](#d-015) | Disponibilità a orari arbitrari (fasce libere) | 🔓 aperta |
| [D-016](#d-016) | Streaming live di log grezzi nella console superuser | 🔓 aperta |
| [D-017](#d-017) | Brand pubblico e dominio definitivo | ✅ chiusa |
| [D-018](#d-018) | Prezzo per tipologia di ombrellone | 🔓 aperta |
| [D-019](#d-019) | Ombrellone *standalone* senza Fila | 🔓 aperta |
| [D-020](#d-020) | Pattern colorblind-safe per gli stati della mappa | 🔓 aperta |
| [D-021](#d-021) | Validazione runtime dei payload nel frontend | 🔓 aperta |
| [D-022](#d-022) | Validazione server-side dell'input API | ✅ chiusa |
| [D-023](#d-023) | Least-privilege del ruolo DB applicativo (`CREATEDB`) | 🔓 aperta |
| [D-024](#d-024) | Cancellazione e anonimizzazione del cliente (GDPR) | 🔓 aperta |
| [D-025](#d-025) | Gestione utenti e RBAC sugli endpoint | 🔓 aperta |
| [D-026](#d-026) | Refresh e revoca dei token | ✅ chiusa |
| [D-027](#d-027) | Rate-limiting e difesa brute-force | ✅ chiusa |
| [D-028](#d-028) | Percorso privilegiato RLS per `User` | 🔓 aperta |
| [D-029](#d-029) | Anti-enumeration e timing-oracle | ✅ chiusa |
| [D-030](#d-030) | Exclusion constraint DB per l'anti-overlap | ✅ chiusa |
| [D-031](#d-031) | Fuso orario configurabile per-tenant | 🔓 aperta |
| [D-032](#d-032) | Editor CRUD del listino | ✅ chiusa |
| [D-033](#d-033) | Pricing di prenotazione periodica multi-stagione | 🔓 aperta |
| [D-034](#d-034) | Prezzo forfait per prenotazione periodica | 🔓 aperta |
| [D-035](#d-035) | Canale cliente + assenze comunicate degli abbonati | ✅ chiusa |
| [D-036](#d-036) | Report cruscotto avanzato (heatmap, serie incassi) | 🔓 aperta |
| [D-037](#d-037) | Gestione globale del `401` nel data-layer frontend | ✅ chiusa |
| [D-038](#d-038) | Drag-reorder e re-parent nell’editor struttura | 🔓 aperta |
| [D-039](#d-039) | Drift Prisma su `Rate_signature_key` | ✅ chiusa |
| [D-040](#d-040) | Estrazione di `EstablishmentStructureView.vue` | 🔓 aperta |
| [D-041](#d-041) | Filtro globale Prisma: `P2002`→409 e `P2023`→400 | ✅ chiusa |
| [D-042](#d-042) | Impersonation e accesso PII puntuale per il supporto | 🔓 aperta |
| [D-043](#d-043) | Vista materializzata `establishment_metrics` | 🔓 aperta |
| [D-044](#d-044) | `User.lastLoginAt` — tracciamento del login reale | 🔓 aperta |
| [D-045](#d-045) | Rimozione dello stub console superuser da `web-staff` | ✅ chiusa |
| [D-046](#d-046) | Visibilità della deliverability dell'invito | 🔓 aperta |
| [D-047](#d-047) | Audit di tenant per le azioni admin-in-tenant | 🔓 aperta |
| [D-048](#d-048) | Fasce orarie sovrapposte e stato mappa `covered` | ✅ chiusa |
| [D-049](#d-049) | `testTimeout` esplicito nella config e2e | ✅ chiusa |
| [D-051](#d-051) | UI operatore per il provisioning dell'accesso cliente | ✅ chiusa |
| [D-052](#d-052) | Unità fisiche numerate e rientro parziale dei noleggi | 🔓 aperta |
| [D-053](#d-053) | Overtime automatico e billing a durata reale | 🔓 aperta |
| [D-054](#d-054) | Bottom-sheet trascinabile per il pannello mappa | 🔓 aperta |
| [D-055](#d-055) | «Ritira ombrellone» (soft-delete) e storico | ✅ chiusa |
| [D-056](#d-056) | MapView discrimina il settore «Speciali» per nome | ✅ chiusa |
| [D-057](#d-057) | I tab settore del Cantiere non dichiarano un `tabpanel` | ✅ chiusa |
| [D-058](#d-058) | FK opzionali di `Rate` con `ON DELETE SET NULL` | ✅ chiusa |
| [D-059](#d-059) | Relation opzionali residue con `ON DELETE SET NULL` implicito | 🔓 aperta |
| [D-060](#d-060) | `umbrellaLabel` non risolve gli ombrelloni ritirati | ✅ chiusa |
| [D-061](#d-061) | Privacy policy operatori + cookie/imprint app interne | 🔓 aperta |
| [D-062](#d-062) | DPA Coralyn↔lido e registro dei trattamenti | 🔓 aperta |
| [D-063](#d-063) | Permessi dello staff configurabili dall'admin del lido | 🔓 aperta |
| [D-064](#d-064) | `GET /establishment/overview` espone le email degli operatori | ✅ chiusa |
| [D-065](#d-065) | Package frontend condiviso per il data-layer | ✅ chiusa |
| [D-066](#d-066) | `pnpm run verify` va in OOM sotto pressione di memoria | ✅ chiusa |
| [D-067](#d-067) | Budget esplicito di transazione e di pool al confine di piattaforma | 🔓 aperta |
| [D-068](#d-068) | `packages/contracts/dist` è tracciato, in CRLF e fuori da `.gitattributes` | 🔓 aperta |
| [D-069](#d-069) | L'indice degli ADR non ha un presidio e deriva a ogni ADR nuovo | 🔓 aperta |

## Aperte

| ID | Tema | Perché rimandata | Cosa la sblocca / trigger | Impatto se ignorata |
|---|---|---|---|---|
| <a id="d-002"></a>D-002 | Infrastruttura multi-tenancy completa (signup self-service, isolamento avanzato, billing) | Il Core viene costruito tenant-aware nel modello dati, ma l'infra SaaS completa è il modulo 3. Il provisioning attuale è **fornitore + inviti** ([ADR-0028](decisions/0028-provisioning-tenant.md)): la self-registration aperta è rifiutata, non solo rimandata. **NB (2026-07-23):** l'**onboarding guidato di prima configurazione** ([ADR-0054](decisions/0054-onboarding-incrementale-setup-status.md)) NON è self-service signup: configura un lido **già provisionato** dal fornitore, quindi non intacca questa voce. | Domanda di onboarding self-service con trial/billing. | Se il modello dati non è tenant-aware, riscrittura costosa (mitigato: lo è da subito). |
| <a id="d-003"></a>D-003 | Internazionalizzazione UI (i18n) | UI in italiano per il mercato iniziale. | Espansione su mercati non italofoni. | Retrofit i18n possibile ma oneroso se non previsto nei pattern UI. |
| <a id="d-004"></a>D-004 | Pagamenti online / integrazione POS e fiscale (scontrino elettronico) | Appartiene al modulo Cassa, dopo il Core. | Inizio modulo Cassa e pagamenti. | Vincoli fiscali italiani vanno studiati per tempo (corrispettivi telematici). |
| <a id="d-005"></a>D-005 | Editor planimetria libero della mappa (opzione C: coordinate libere, drag&drop su foto/planimetria — layer di **presentazione**, `Umbrella.presentationPosition`) | L'MVP adotta il modello logico a settori/file (opzione B), sufficiente per quasi tutti i lidi. L'editor a coordinate aggiunge complessità non necessaria ora. **Aggiornamento 2026-07-04:** l'editor **logico** della struttura (settori/file/ombrelloni/tipologie = il bottone «Configura» dello Stabilimento) NON è più deferito: è la spec [2026-07-04-stabilimento-configura-struttura-design.md](../superpowers/specs/2026-07-04-stabilimento-configura-struttura-design.md) (in costruzione a slice). D-005 resta **solo** il layer pixel/coordinate libere. | Domanda di mercato per planimetria fedele al pixel, dopo l'MVP del Core. | Basso: il modello mappa separa la posizione *logica* dalla *presentazione*, quindi l'editor sarà additivo, non una riscrittura. |
| <a id="d-006"></a>D-006 | Liste d'attesa avanzate: hold temporanei con scadenza automatica + notifiche al cliente | L'MVP fa coda + promozione manuale; hold e notifiche dipendono dal modulo notifiche/booking online. | Modulo notifiche o Booking online (modulo 4). | Media: senza hold automatici la gestione dei picchi resta manuale. |
| <a id="d-007"></a>D-007 | Wrapper desktop Electron | La delivery primaria è web + PWA ([ADR-0004](decisions/0004-form-factor-e-delivery.md)); un client desktop nativo non serve ora. | Richiesta esplicita di un client desktop nativo. | Bassa: additivo, riusa la stessa web app. |
| <a id="d-008"></a>D-008 | Offline-sync completo della PWA | Complesso (sincronizzazione e risoluzione conflitti); l'MVP assume connettività con offline-light (shell + consultazione in cache). | Problemi reali di connettività in spiaggia. | Media: in spiaggia la rete può mancare; mitigato dall'offline-light. |
| <a id="d-009"></a>D-009 | Entità `Pagamento` completa (acconti multipli, ricevute, storni/rimborsi) | L'MVP registra l'incasso base sulla Prenotazione ([ADR-0011](decisions/0011-incasso-base-nel-core.md)); il modello ricco appartiene alla Cassa. | Inizio modulo Cassa (modulo 2). | Bassa/Media: migrazione contenuta dai campi base all'entità `Pagamento`. |
| <a id="d-010"></a>D-010 | Isolamento fisico per tenant grandi (ibrido pool + silo: DB dedicato) | L'MVP usa shared schema + RLS ([ADR-0010](decisions/0010-isolamento-multi-tenant.md)), adatto a molti tenant piccoli/medi. | Tenant grande con esigenze di carico, compliance o on-prem. | Bassa: l'app filtra sempre per tenant, la promozione a DB dedicato non tocca il codice. |
| <a id="d-012"></a>D-012 | Cabina e servizi accessori (posto auto, ingressi) come risorse prenotabili | L'MVP gestisce gli ombrelloni; la Cabina è una risorsa gemella aggiungibile col medesimo pattern. | Lidi per cui cabine/servizi sono centrali nell'offerta. | Bassa: additivo (stesso pattern di Ombrellone), non una riscrittura. |
| <a id="d-014"></a>D-014 | Gestione personale / turni bagnini (rostering, presenze) | Sotto-dominio HR a basso accoppiamento col Core, valore incerto; nel Core resta solo l'`Utente` per l'accesso. | Domanda reale dei clienti → possibile modulo successivo. | Bassa: indipendente dal cuore prenotazioni, costruibile a parte. |
| <a id="d-015"></a>D-015 | Disponibilità a orari arbitrari (fasce libere, es. 10–13) | L'MVP usa slot configurabili intera/mezza giornata ([ADR-0013](decisions/0013-granularita-disponibilita-a-slot.md)); l'orario libero è atipico per ombrelloni e costoso. | Domanda concreta di prenotazioni a ore. | Bassa: il modello a `Fascia` è generalizzabile a fasce più fini senza riscrittura. |
| <a id="d-016"></a>D-016 | Streaming live di log tecnici grezzi nella console superuser | L'MVP mostra audit + errori sanificati ([ADR-0015](decisions/0015-osservabilita-e-console-superuser.md)); il live raw è rischioso (PII) e costoso. | Esigenza diagnostica avanzata. | Bassa: si appoggia allo stack di logging già presente. |
| <a id="d-018"></a>D-018 | Prezzo per tipologia di ombrellone (tipo come dimensione della `TARIFFA`) | L'MVP prezza per posizione (Settore/Fila, [ADR-0006](decisions/0006-dominio-prenotazioni-e-pricing.md)); nel caso reale il tipo coincide con la fila/zona, quindi basta. Vedi [ADR-0016](decisions/0016-tipologia-ombrellone.md). | Un lido con tipi a prezzo diverso **nella stessa fila**. | Bassa: additivo, `Tipologia` è già modellata; si aggiunge un `ambito` al pricing engine. |
| <a id="d-019"></a>D-019 | Ombrellone *standalone* senza Fila (posizionamento libero a livello di modello) | L'MVP tiene ogni `Ombrellone` in una `Fila`; gli speciali fuori griglia stanno in un Settore/Fila dedicato ([ADR-0016](decisions/0016-tipologia-ombrellone.md)). | Esigenza di ombrelloni del tutto fuori dalla struttura a file. | Bassa: rilassare `fila_id` a nullable o agganciare al Settore è una migrazione contenuta. |
| <a id="d-020"></a>D-020 | Modalità a pattern colorblind-safe per gli stati della mappa | L'MVP usa **colore + testo/`aria-label`** ([ADR-0018](decisions/0018-linguaggio-visivo.md), [ADR-0020](decisions/0020-resa-mappa.md)), leggibile dagli screen reader; il pattern aggiunge rumore visivo e complessità di rendering. | Utenti daltonici che faticano sulla mappa, o requisito d'accessibilità formale. | Bassa: additivo (overlay di pattern sulle celle; i token di stato restano). |
| <a id="d-021"></a>D-021 | Validazione runtime dei payload FE (es. `zod` ai bordi del data-layer) | I `contracts` sono tipi TS (compile-time) e il confine FE/BE è tipizzato; nello slice 1 la validazione runtime è YAGNI ([ADR-0021](decisions/0021-server-state-frontend.md)). | Payload non fidati (input utente complessi) o drift osservato tra API reale e DTO. | Bassa: additivo — `parse` dei DTO nei composable di TanStack Query, senza toccare i componenti. |
| <a id="d-023"></a>D-023 | Least-privilege del ruolo DB applicativo (`coralyn_app` ha `CREATEDB`) | `CREATEDB` serve solo allo shadow DB di `prisma migrate dev` in sviluppo; in produzione si usa `migrate deploy` (niente shadow). Sul container dev locale il rischio è nullo (password nota). | Primo ambiente condiviso/staging/prod, o CI con DB gestito. | Bassa: separare il ruolo di migrazione (o configurare `shadowDatabaseUrl`) e revocare `CREATEDB` dal ruolo applicativo. |
| <a id="d-024"></a>D-024 | Cancellazione / anonimizzazione del `Cliente` (GDPR) | L'Incremento 1 della scheda cliente fa crea/leggi/modifica, **niente DELETE**; manca ancora il legame `Cliente`↔`Prenotazione`/storico che renderebbe l'hard-delete inammissibile. Minimizzazione già applicata (solo telefono/email/note; niente documento d'identità). Vedi [ADR-0023](decisions/0023-contatti-cliente-colonne-tipizzate.md). **CORE FATTO 2026-07-06:** il meccanismo di erasure è **realizzato** — spec [2026-07-06-gdpr-customer-erasure-d024-design.md](../superpowers/specs/2026-07-06-gdpr-customer-erasure-d024-design.md), [ADR-0043](decisions/0043-erasure-e-retention-cliente-gdpr.md): `DELETE /api/customers/:id` admin-only ([ADR-0039](decisions/0039-rbac-role-guard.md)) — 0 prenotazioni → delete reale; con storico → anonimizzazione in place irreversibile (`firstName`/`lastName` → placeholder, `phone`/`email`/`notes` → null); almeno una prenotazione `confirmed` futura → **409** (oblio differito, non negato); `list()` esclude gli anonimizzati; accountability minima via `anonymizedAt`/`anonymizedBy`. **Resta deferito** solo il **consenso/informativa** (Art. 13 GDPR) alla creazione del cliente. L'audit completo delle azioni admin-in-tenant resta in **D-047**. **Aggiornamento 2026-07-24 (piano A — FATTO):** il residuo "informativa Art. 13 alla raccolta" è ora **realizzato per il piano A** (informativa al bagnante, parametrizzata per-lido via `EstablishmentLegalProfile`, 1:1 RLS; base giuridica = contratto/obbligo legale, **nessun consenso**; testo come codice versionato in web-customer, titolare come dato) — spec [2026-07-24-privacy-informativa-art13-5-6a-design.md](../superpowers/specs/2026-07-24-privacy-informativa-art13-5-6a-design.md), [ADR-0055](decisions/0055-informativa-art13-multi-tenant.md). **Restano deferiti i piani B e C** (privacy policy operatori/cookie/imprint con Coralyn titolare; DPA Art. 28 + registro trattamenti Art. 30), tracciati **non silenziosamente** come nuove voci **D-061**/**D-062** (slice 5.6b / 5.6c) qui sotto. | ~~Consenso/informativa: slice futura dedicata alla base giuridica di raccolta~~ **FATTO per il piano A** (2026-07-24, [ADR-0055](decisions/0055-informativa-art13-multi-tenant.md)); resta il trigger di D-061/D-062 (slice 5.6b/5.6c: onboarding di un lido reale, primo contratto, richiesta di audit). | ~~Bassa~~ **piano A CHIUSO**: informativa reale, parametrizzata per-lido, nessuna checkbox di consenso impropria. Restano D-061/D-062 (slice 5.6b/5.6c), vedi voci dedicate. |
| <a id="d-025"></a>D-025 | Gestione utenti & RBAC sugli endpoint (admin crea/elenca/disabilita staff) | **CORE FATTO 2026-07-04** (mergiato su `main`): role-guard applicativo `@Roles`/`RolesGuard` ([ADR-0039](decisions/0039-rbac-role-guard.md)) + gestione utenti — `POST`/`PATCH /api/establishment/users/:id` admin-only (crea staff con email unica→409 e ruolo≠superuser→400; disabilita/riabilita soft via `User.disabledAt` con invarianti anti-lockout no-self/no-ultimo-admin→422); il `login` respinge i disabilitati; overview espone `disabledAt`. **Aggiornamento 2026-07-05:** il meccanismo di **invito via email** (provisioning nuovo admin + reset-password admin) è ora **realizzato** — spec [2026-07-05-credential-invite-email](../superpowers/specs/2026-07-05-credential-invite-email-design.md), [ADR-0042](decisions/0042-trasporto-email-e-consegna-credenziali.md): token opaco set-password via Mailpit/SMTP, nessuna password in chiaro. **Aggiornamento 2026-07-05 (slice invito staff):** l'**invito staff** (creazione via link set-password) e il **reset-password staff** tenant-scoped sono ora **realizzati** — spec [2026-07-05-staff-invite-email](../superpowers/specs/2026-07-05-staff-invite-email-design.md), riusa [ADR-0042](decisions/0042-trasporto-email-e-consegna-credenziali.md); il campo password è rimosso dalla form e da `CreateStaffUserInput`. **Resta deferito** solo il **cambio-ruolo** di un utente esistente. L'audit delle azioni admin-in-tenant è tracciato in **D-047**. | Cambio-ruolo: richiesta operativa. | Bassa: additivo sul modello già presente (nessuna riscrittura). |
| <a id="d-028"></a>D-028 | Percorso privilegiato RLS per `User` (difesa-in-profondità oltre il choke point) | `User` non ha RLS per scelta ([ADR-0026](decisions/0026-identita-rls-utente.md)); l'accesso è mediato solo a livello applicativo. **Aggiornamento 2026-07-15 (D-035 S3):** valutato durante l'auth del canale cliente ([ADR-0049](decisions/0049-auth-cliente-provisioned-tenant-pubblico.md)) e **confermato non-trigger** — il canale cliente introduce due nuove tabelle fuori-RLS (`CustomerEnrollmentToken`/`CustomerSession`) con **accessore applicativo unico** (`customer-auth`), stesso pattern `CredentialSetupToken`, senza aumentare gli accessori di `User`. Resta tracciato come hardening futuro. | Più accessori della tabella identità o requisito di hardening formale. | Bassa: oggi accessori unici per tabella; additivo in futuro. |
| <a id="d-031"></a>D-031 | Fuso orario configurabile per-tenant (colonna `timezone` sullo `Establishment`) | L'MVP è mono-mercato: fuso fisso `Europe/Rome` ([ADR-0031](decisions/0031-fuso-orario-e-date-operative.md)). | Stabilimento in un fuso diverso, o espansione internazionale (lega a [D-003](deferred.md) i18n). | Bassa: additivo (una colonna + lettura del fuso nelle utility di data già centralizzate). |
| <a id="d-033"></a>D-033 | Pricing di prenotazione **periodica multi-stagione** (intervallo a cavallo di due `Season` con listini diversi → split per stagione + somma) | A4.1 risolve la stagione da `startDate` e **rifiuta con 422** un periodo che sfora `season.endDate` ([ADR-0006](decisions/0006-dominio-prenotazioni-e-pricing.md)); nell'anno operativo le stagioni non sono contigue (il lido chiude tra una e l'altra), quindi il caso è atipico e lo split multi-stagione sarebbe gold-plating. | Un lido con stagioni contigue/adiacenti e domanda reale di periodiche a cavallo. | Bassa: additivo — l'engine è puro e per-stagione; lo split sommerebbe i prezzi delle stagioni attraversate senza cambiare le precedenze ([ADR-0032](decisions/0032-pricing-engine-precedenza.md)). |
| <a id="d-034"></a>D-034 | Prezzo **forfait per prenotazione periodica** (pacchetto-periodo a prezzo fisso, es. "settimana secca 200€", indipendente dal numero di giorni) | Lo slice "Chiarezza tipi prenotazione" fissa che il **periodico è sempre a giornata** (`price × giorni`) e **rimuove il campo `unit`** dalla `Rate`: il calcolo si deriva dal `type` (giornaliera/periodica → ×giorni; abbonamento → forfait). Il forfait-periodo è un caso reale ma **non richiesto ora** ("per ora", utente 2026-07-02). | Domanda concreta di pacchetti-periodo a prezzo fisso (deal settimanali/mensili a forfait). | Bassa: additivo — reintroducibile senza rompere il modello (es. un forfait a livello di `Package`, o re-introdurre una dimensione/flag di prezzo dedicata), senza toccare le precedenze [ADR-0032](decisions/0032-pricing-engine-precedenza.md). |
| <a id="d-036"></a>D-036 | **Report cruscotto avanzato** (heatmap occupazione, occupazione media di periodo, serie incassi a bucket settimanali per la stagione, date arbitrarie/export CSV-PDF/persistenza del periodo scelto, azione di **rinnovo inline** nel Report) | Lo slice Report v1 (spec [2026-07-04-report-cruscotto-design.md](../superpowers/specs/2026-07-04-report-cruscotto-design.md) §7) è consapevolmente minimale: 2 grafici (incassi + donut stati), il periodo guida **solo l'incasso** (occupazione/da-incassare/abbonamenti = snapshot "ora"), `revenueSeries` sempre a 7 giorni giornalieri, scadenze read-only con «Rinnova» che **naviga** al flusso esistente. Le estensioni analitiche (heatmap, medie storiche per range, bucket settimanali stagione) sono costose e di basso valore in v1; l'export e le date libere sono gold-plating; il rinnovo inline duplicherebbe logica già esistente e romperebbe il "read-only". | Domanda concreta di reportistica analitica/interattiva o di operare i rinnovi dal Report. | Bassa: additivo — l'endpoint `GET /reports/summary` è parametrico e i grafici sono incapsulati in wrapper ui-kit ([ADR-0038](decisions/0038-libreria-grafici-echarts.md)); heatmap/serie stagione si aggiungono senza toccare il resto. |
| <a id="d-038"></a>D-038 | **Drag-reorder di settori/file/ombrelloni** nell'editor struttura («Configura»), e **re-parent** (spostare una fila tra settori / un ombrellone tra file) | L'editor struttura v1 ([spec 2026-07-04-stabilimento-configura-struttura](../superpowers/specs/2026-07-04-stabilimento-configura-struttura-design.md)) usa `sortOrder` = **ordine di creazione**; il riordino via drag&drop e lo spostamento tra contenitori non sono nel mockup e sono additivi. | Domanda di riordinare la struttura senza elimina+ricrea. | Bassa: le colonne `sortOrder`/`logicalOrder` esistono già → un endpoint di reorder (batch di `sortOrder`) e il re-parent (cambio `sectorId`/`rowId`) sono additivi, nessuna migrazione strutturale. |
| <a id="d-040"></a>D-040 | **Estrazione di `EstablishmentStructureView.vue`** (editor «Configura») in composabili/child-component per-entità + union condivise | A Configura completo (Slice 1+2+3) la vista ospita 4 entità CRUD (tipologie/settori/file/ombrelloni) + 5 modali + generatore in ~406 righe: ancora leggibile (sezioni `// --- X ---`, naming `open*/submit*/saving*`), ma alla soglia dove un fix cross-entità o uno Slice 4 beneficerebbe di estrazione. Include: `SECTOR_KINDS` (`grid|special`) duplicato nei 2 DTO settore + Select FE, e la union icone (`umbrella|leaf|palmtree`) ripetuta in 4 punti (2 DTO api + 2 nel componente) → esportare `UmbrellaIconKey`/`SectorKind` values condivisi da `@coralyn/contracts`. | Nuova funzionalità sull'editor struttura o attrito di manutenzione sulla vista. | Bassa: refactor puro (composabili `useSectorModal()`/…, o modali in child-component con `v-model`), nessun cambio di comportamento; test già a copertura. |
| <a id="d-042"></a>D-042 | **Impersonation / accesso PII puntuale per supporto** dalla Platform Console (con motivazione obbligatoria + audit di lettura) | La Platform Console ([spec 2026-07-05](../superpowers/specs/2026-07-05-platform-console-superuser-design.md), [ADR-0040](decisions/0040-lettura-aggregata-cross-tenant.md)) applica una **parete rigida**: solo aggregati PII-free, nessun endpoint/vista espone i dati dei bagnanti. Il supporto puntuale a un lido (vedere una specifica prenotazione/cliente) richiederebbe un **escape-hatch controllato** — impersonation del ruolo lido, o lettura mirata — con **motivazione obbligatoria**, **audit di lettura** (oltre a quello di mutazione già presente) e visibilità al titolare. Non necessario finché gli aggregati bastano al distributore. | Richiesta reale di supporto che imponga di vedere un dato puntuale del lido. | Media (a governance, non a rischio dati oggi): additivo, ma va progettato con cura per non erodere la parete GDPR — audit di lettura, scope temporale, consenso/tracciamento verso il titolare. |
| <a id="d-043"></a>D-043 | **Vista materializzata `establishment_metrics`** (upgrade del loop `forTenant` della Platform Console) | Le metriche cross-tenant si calcolano oggi con un **loop `forTenant`** O(N) ([ADR-0040](decisions/0040-lettura-aggregata-cross-tenant.md)), adeguato a decine/centinaia di lidi su una pagina admin non-hot. A scala maggiore, una **vista materializzata** pre-aggregata (rinfrescata periodicamente, PII-free per costruzione) renderebbe la lista O(1) a leggere. Stesso `PlatformEstablishmentDTO` → la console non cambia. | Numero di lidi tale da rendere lento il loop, o esigenza di refresh dashboard frequente. | Bassa: additivo — cambia solo la sorgente del `PlatformMetricsService`, non il DTO né il FE; aggiunge un job di refresh e tollera dati leggermente stantii. |
| <a id="d-044"></a>D-044 | **`User.lastLoginAt`** — tracciamento del login reale | La metrica `lastActivityAt` della Platform Console usa `max(Booking.createdAt)` del lido come **proxy** di operatività, perché oggi il login **non** è tracciato ([spec 2026-07-05](../superpowers/specs/2026-07-05-platform-console-superuser-design.md) §6). Una colonna `User.lastLoginAt` scritta sul percorso di login darebbe il segnale di attività reale (incluso "l'admin entra ma non prenota"). | Esigenza di distinguere "lido attivo ma non opera prenotazioni" da "lido morto", o analitica sessioni. | Bassa: additivo — una colonna + una scrittura su `IdentityService.login`; `lastActivityAt` diventa `max(lastLoginAt, Booking.createdAt)`. |
| <a id="d-046"></a>D-046 | **Visibilità della deliverability dell'invito nella Platform Console** (feedback esplicito se l'email set-password non parte) | L'invio è **best-effort** ([ADR-0042](decisions/0042-trasporto-email-e-consegna-credenziali.md)): un fallimento SMTP logga un WARN ed è recuperabile via reset-admin-password, ma la console riporta comunque successo con `expiresAt`. Per un MVP interno con un solo distributore il log server basta. | Esposizione pubblica, più operatori, o richiesta di conferma della consegna. | Bassa: un misconfig SMTP sistematico è visibile solo nei log del server, non in console; il recupero via reset esiste comunque. Additivo: un flag `emailSent` in `CreateEstablishmentResponse`/`ResetAdminPasswordResponse` + banner nella console. |
| <a id="d-047"></a>D-047 | **Audit di tenant per le azioni admin-in-tenant** (invito/reset credenziali staff, create/disable/enable staff, futuro cambio-ruolo) | `reset-admin-password` usa `PlatformAuditLog` che è scope **superuser**; non esiste un audit-log **di tenant**. Introdurlo per coprire *solo* invito/reset staff creerebbe una nuova incoerenza (le altre mutazioni admin resterebbero non auditate): l'audit di tenant è una slice a sé, non un bolt-on alla storia credenziali. Traccia tecnica parziale già presente: `CredentialSetupToken.createdByUserId` + `purpose` + timestamp per invito/reset. | Esposizione multi-operatore, o richiesta di tracciabilità/compliance a livello lido. | Bassa: la traccia token esiste per le azioni credenziali; le altre mutazioni restano non auditate. Additivo: tabella dominio RLS-FORCE tenant-scoped + service + scrittura atomica nella tx dell'azione + eventuale **ADR dedicato**, coprendo **tutte** le mutazioni admin. |
| <a id="d-052"></a>D-052 | **Unità fisiche numerate + rientro parziale/sfalsato** di un gruppo di noleggi | Il banco noleggio ([ADR-0050](decisions/0050-noleggio-mezzi-servizi.md)) modella `Rental.units` come **conteggio** (scorta informativa) e il rientro è **tutto-o-niente** sul noleggio: un gruppo che prende 5 pedalò è una riga sola, resa insieme. Numerare i singoli esemplari (pedalò #1..#5) e gestirne il rientro sfalsato è gold-plating senza richiesta. | Domanda reale di tracciare il singolo esemplare o di rendere una parte del gruppo prima del resto. | Bassa: additivo — tabella di unità fisiche + rientro per-unità, senza toccare il modello a conteggio. |
| <a id="d-053"></a>D-053 | **Overtime automatico / billing a durata reale** del noleggio | Le tariffe ([ADR-0050](decisions/0050-noleggio-mezzi-servizi.md)) sono a **prezzo fisso a scaglioni** (30min/1h/giornata): `totalPrice = tariff.price × units`, snapshot al checkout; `returnedAt` serve a disponibilità/storia ma **non** ricalcola il prezzo. Il conteggio al minuto e il sovrapprezzo per ritardo sono reali ma non richiesti ora. | Domanda di addebito a consumo reale (durata effettiva) o penale di ritardo. | Bassa: additivo — calcolo a durata su `startAt`/`returnedAt` con arrotondamento, senza toccare le tariffe a scaglioni. |
| <a id="d-054"></a>D-054 | **Bottom-sheet trascinabile per il pannello mappa in layout compatto** (`< lg`) | [ADR-0051](decisions/0051-responsive-drawer-e-telefono-graceful.md) ratifica il pannello **impilato** sotto la mappa (slice responsive 2026-07-21): funziona ed è senza debiti; il bottom-sheet (previsto da [ADR-0019](decisions/0019-app-shell-e-ux.md) per il tablet) è una miglioria ergonomica, non un requisito. | Feedback reale degli operatori su tablet (dettaglio ombrellone scomodo da raggiungere con lo scroll). | Bassa: additivo — un primitivo bottom-sheet in `ui-kit` (stesso pattern headless di `NavDrawer`) montabile in `MapView` senza toccare il layout esteso. |
| <a id="d-061"></a>D-061 | 5.6b — Privacy policy **operatori** (Coralyn titolare) + cookie/imprint per `web-staff`/`web-platform` | Verso l'**operatore** (utente delle app interne), il titolare del trattamento è **Coralyn** stessa, non il lido: è un piano GDPR distinto dall'informativa al bagnante (5.6a, [ADR-0055](decisions/0055-informativa-art13-multi-tenant.md)) e richiede un proprio testo (privacy policy SaaS B2B, con Coralyn come titolare) + cookie/imprint delle app interne. Tenuto fuori da 5.6a apposta, per non mescolare i due piani titolare/responsabile nella stessa slice — [ADR-0055](decisions/0055-informativa-art13-multi-tenant.md) ne discute l'estrazione a package legale condiviso, rimandata anch'essa qui. **Aggiornamento 2026-07-24 (contenuti REDATTI, pubblicazione NON fatta):** le bozze esistono in [`docs/legal/`](../legal/README.md) — [privacy policy operatori](../legal/privacy-policy-operatori.md) (Coralyn titolare) e [imprint](../legal/imprint.md) ex art. 7 D.Lgs. 70/2003. **Verificato sul codice, non supposto:** `web-staff`/`web-platform` non hanno **alcun** cookie (zero occorrenze di `document.cookie`/`Set-Cookie` nel repo), nessun analytics, nessuno script di terze parti, e i font sono un pacchetto npm locale (nessuna chiamata a Google Fonts) → ⚠️ **corretto il 2026-07-26 (Fase H): questa riga diceva «l'unica memorizzazione è il token di sessione in `localStorage`», ed era FALSA.** Le memorizzazioni sul dispositivo sono **due categorie**: i token di sessione in `localStorage` (uno in `web-staff` e `web-platform`, **due** in `web-customer`) **e** la cache degli asset del service worker — tutte e tre le app sono PWA con precaching Workbox (`vite-plugin-pwa`). Verificato sul codice: `globPatterns` copre soltanto `js/css/html/svg/png/woff2` e **non esiste alcun `runtimeCaching`**, quindi nessuna risposta API e nessun dato personale finisce in cache. Entrambe le categorie ricadono nell'art. 5(3) ePrivacy (EDPB Orientamenti 2/2023: norma **neutra rispetto alla tecnologia** — «non sono cookie» non basta), ed entrambe sono ⚖️ **strettamente necessarie** al servizio richiesto → **niente banner di consenso**, solo una sezione informativa. **La conclusione regge, ma non per la ragione che questa riga dava**, ed è questo il punto: l'analisi corretta e autorevole è in [`docs/legal/README.md`](../legal/README.md) §1, che elenca entrambe le categorie dal 2026-07-24 — è questa voce del registro a essere rimasta indietro. La conclusione **decade** se si aggiunge un analytics, una mappa, un video incorporato, un font da CDN **o una strategia di caching runtime verso un dominio terzo**. **Restano aperti:** (a) i dati societari di Coralyn, inesistenti → `[COMPILARE]` ovunque; (b) la **decisione strutturale su dove pubblicare** il testo nelle due app (duplicato per app vs package condiviso `@coralyn/legal`, alternativa già discussa in [ADR-0055](decisions/0055-informativa-art13-multi-tenant.md)) → **serve un ADR dedicato**; (c) la revisione legale dei punti ⚖️. **Aggiornamento 2026-07-24 (PUBBLICATA — nuovo [ADR-0056](decisions/0056-package-legale-condiviso.md)):** la decisione strutturale rimandata da ADR-0055 è ora **presa**: nuovo package **`@coralyn/legal`** (`packages/legal`) con i contenuti versionati e i componenti, consumato da **entrambe** le app. Motivo: il contenuto del piano B è **identico** per `web-staff` e `web-platform` (titolare sempre Coralyn, nessuna parametrizzazione per tenant), quindi duplicarlo venderebbe la possibilità di una **divergenza silenziosa tra due documenti legali** — un rilievo di compliance, non un difetto estetico. Rotte **`/legale/informativa`** e **`/legale/note`** in entrambe, **pubbliche di proposito** (art. 7 D.Lgs. 70/2003 impone accessibilità «diretta e permanente»; art. 14.3(a) GDPR impone di rendere l'informativa a chi non ha ancora un account), con test per app che vincolano la proprietà — il routing è l'unico punto residuo in cui le due app potrebbero divergere. Link nel piè di pagina di entrambe le schermate di login. **Aggiornamento 2026-07-25 (correzione di un difetto introdotto dalla slice stessa):** le rotte operatori erano inizialmente su `/privacy`, **lo stesso path dell'informativa al bagnante** servita da `web-customer`. Combinato col fallback a percorso **relativo** di `privacyPreviewUrl` (attivo quando `VITE_WEB_CUSTOMER_URL` manca), l'operatore che apriva l'anteprima restava sull'origin di `web-staff` e vedeva **la policy operatori al posto dell'informativa del cliente**, con `?e=` ignorato e nessun errore. Prima della rotta quel link falliva in modo visibile: la rotta ha reso l'errore silenzioso. Rimedi (tutti e tre necessari): path distinti sotto `/legale/` con test che vietano la ricomparsa di `/privacy`; **eliminato il fallback relativo** (senza la env il promemoria resta testo **senza link**); **`strictPort`** nei tre `vite.config.ts` (5173/5174/5175), perché senza era l'ordine di avvio a decidere quale app stesse su quale porta. Vedi [ADR-0056](decisions/0056-package-legale-condiviso.md) §3-bis. **Aggiornamento 2026-07-24 (⚖️-18 CHIUSO):** il template email (`credential-setup.email.ts`) rinvia ora all'informativa in **testo e HTML**, per **invito e reset**, sulla stessa origin del link di set-password (nessuna env aggiuntiva). L'art. 14.3(a) è assolto: i dati dell'operatore non li conferisce lui ma il suo datore di lavoro, quindi l'informativa va **resa**, e quell'email è il primo contatto con l'interessato. Quattro test la vincolano, incluso uno che verifica l'assenza di `<img>` (provv. Garante 284/2026 sui tracking pixel). **Restano aperti:** i dati societari (`[COMPILARE]`) e i punti ⚖️ residui, entrambi fuori dal controllo tecnico. **D-061 CHIUSA sul piano tecnico.** | Onboarding di un primo lido reale, o richiesta legale di pubblicare le policy interne prima dell'esposizione pubblica. | ~~Media~~ **Bassa**: contenuto redatto, revisionato e **pubblicato** in entrambe le app da fonte unica; restano dati societari e validazione legale, entrambi fuori dal controllo tecnico. |
| <a id="d-062"></a>D-062 | 5.6c — DPA Coralyn↔lido (Art. 28 GDPR) + registro dei trattamenti (Art. 30 GDPR), come documenti in `docs/legal/` | La qualificazione titolare (lido) / responsabile (Coralyn) ratificata da [ADR-0055](decisions/0055-informativa-art13-multi-tenant.md) va **formalizzata contrattualmente** (DPA ex Art. 28, con le garanzie/istruzioni del responsabile) e il trattamento **censito** (registro ex Art. 30); nessuno dei due documenti esiste oggi. Fuori scope di 5.6a, che copre solo l'informativa al bagnante, non il rapporto contrattuale Coralyn↔lido. **Aggiornamento 2026-07-24 (bozze REDATTE):** entrambi i documenti esistono in [`docs/legal/`](../legal/README.md) — [DPA art. 28](../legal/dpa-coralyn-lido.md) (istruzioni documentate, sub-responsabili con autorizzazione generale e preavviso 30gg, notifica breach entro 24h, sorte dei dati a fine rapporto, audit, + Allegato A misure art. 32) e [registro art. 30](../legal/registro-trattamenti.md) (§A 4 trattamenti da **titolare**, §B 2 da **responsabile**, §C misure, §D punti aperti). L'esenzione art. 30.5 sotto i 250 dipendenti **non si applica** (trattamento non occasionale). Le misure di sicurezza sono **verificate sul codice** (RLS `FORCE` + ruolo `NOBYPASSRLS`, argon2id, token solo-hash, refresh rotante con theft-detection, parete PII-free della console) e non dichiarative. **Restano aperti:** dati societari di Coralyn e del lido (`[COMPILARE]`), sub-responsabili (hosting/email non ancora scelti → blocca anche i trasferimenti extra-SEE), termini di retention di log/token/backup, e la revisione legale degli 11 punti ⚖️ riepilogati nel [README](../legal/README.md). **D-062 NON è chiusa**: bozze pronte alla revisione legale, non alla firma. | Primo contratto con un lido reale, o richiesta di audit/compliance formale (anche in vista di D-061, slice 5.6b). | ~~Media~~ **Ridotta**: la qualificazione è ora formalizzata in una bozza contrattuale e il trattamento è censito; resta la validazione legale e i dati mancanti. |
| <a id="d-067"></a>D-067 | **Budget esplicito di transazione e di pool al confine di piattaforma** (`transactionOptions` su `forTenant`, `connection_limit` nella `DATABASE_URL`, timeout SMTP, cap di paginazione) | È la **radice n. 3** di [P9](../audit/findings/P9-performance.md): quattro soglie operative delegate ai default impliciti di tre librerie diverse, cioè **il caso peggiore non è definito da nessuna parte**. Tenuta fuori da [ADR-0062](decisions/0062-generate-ombrelloni-scrittura-batch.md) di proposito: lì serviva togliere 499 round-trip, non alzare un tetto, e col batch il margine sul default è **un fattore 10** (458 ms su 5.000 a RTT 30 ms) — includerla avrebbe fatto passare una decisione che tocca **ogni** transazione dell'API come corollario di un bugfix su un endpoint. Va decisa insieme, con la sua misura: `transactionOptions` e `connection_limit` interagiscono con [D-043](#d-043) e con P9-010 (il limite che si incontra per primo non è la latenza, è il pool). | Primo deploy su infrastruttura gestita, o un secondo P2028 su una transazione diversa dal generatore. | Media: finché ogni transazione resta corta il default regge, ma il primo sintomo di un tetto sbagliato è un **errore secco intermittente**, non un rallentamento — cioè la classe di guasto più cara da diagnosticare. |
| <a id="d-068"></a>D-068 | **`packages/contracts/dist` è tracciato da git, committato in CRLF e non coperto da `.gitattributes`** | `**/dist/` è in `.gitignore`, ma quei tre file furono aggiunti con `-f` e da allora l ignore non li tocca più. Con `core.autocrlf=true` (default di sistema su Windows) il clean filter li normalizza a LF mentre il blob in HEAD ha CRLF: risultato, **ogni `pnpm install` lascia l albero sporco** con un `git status` che dice `M` e un `git diff` **vuoto**. Non è un guasto, è rumore permanente — ma è il tipo di rumore che fa passare inosservato un `git add -A` distratto, che ci infilerebbe un churn di sole line ending su 800+ righe. Tre vie: `packages/contracts/dist/** -text` in `.gitattributes` (minima), normalizzare i blob a LF (una volta), oppure **smettere di tracciare `dist`** e capire perché fu tracciato — nessuno degli altri pacchetti lo fa. | Un diff sospetto su `dist`, o il primo consumatore esterno del pacchetto compilato. | Bassa oggi, ma è una trappola per chi arriva nuovo: il repo sembra sporco appena installato, e il gate non se ne accorge perché il contenuto è identico. |
| <a id="d-069"></a>D-069 | **L indice degli ADR in [`docs/architecture/README.md`](README.md) non ha alcun presidio** | È derivato **quattro volte di fila** senza che nulla lo dicesse: si era fermato a ADR-0051 (rilevato dall audit come AUD-023), poi risistemato fino a 0058, poi di nuovo indietro di quattro (0059, 0060, 0061, 0062) — rimesso a mano il 2026-07-27. È esattamente la domanda del §5a: non «è ancora vero?» ma «cosa lo renderebbe rosso se smettesse di esserlo?». Il presidio è ~20 righe in `packages/docs-lint`, sul modello di [`deploy-guide.spec.ts`](../../packages/docs-lint/src/deploy-guide.spec.ts): elenca i file `decisions/*.md` (escluso il template `0000`), elenca le righe `- [ADR-` del README, e pretende che i due insiemi coincidano — nominando i mancanti. Tenuto fuori da [ADR-0062](decisions/0062-generate-ombrelloni-scrittura-batch.md) perché è igiene documentale, non AUD-022. | Il prossimo ADR scritto: senza il presidio l indice tornerà indietro di uno. | Bassa per il prodotto, media per chi legge: l indice è il punto d ingresso documentato alle decisioni, e un indice incompleto è peggio di uno assente perché sembra completo. |

### Voci estese

Non stanno in una cella di tabella: contengono elenchi, blocchi di codice o tabelle proprie.

- <a id="d-059"></a>**D-059** (aperta) — **Relation opzionali residue con `ON DELETE SET NULL` implicito**, esito dell'audit 2026-07-23 richiesto da D-058. Stessa classe di rischio ma mai decise esplicitamente, quindi **non cambiate d'autorità**: **`Umbrella.umbrellaTypeId`** (la guardia 409 delle tipologie conta gli ombrelloni, anche ritirati; nella finestra di race un ombrellone perderebbe la tipologia in silenzio) e **`Booking.packageId`** (guardia 409 in `deletePackage` conta rate+booking; nella race una prenotazione perderebbe il pacchetto — nota: l'e2e cleanup e il seed non ne dipendono, verificato col full-run verde post-D-058). Caso a parte: **`Rental.customerId`** — l'erasure GDPR ([ADR-0043](decisions/0043-erasure-e-retention-cliente-gdpr.md), `customers.service.remove`) conta solo le `Booking`: un cliente con noleggi e 0 prenotazioni viene hard-deletato e i suoi noleggi perdono il riferimento (di fatto anonimizzati). Plausibilmente desiderabile in ottica GDPR, ma è un **comportamento emergente, non una decisione**. Deliberati e ok: `CustomerSession.rotatedFromId` (SetNull esplicito, catena di rotazione), `Umbrella.rowId` (Restrict, ADR-0053), `User.establishmentId` e `Booking.previousBookingId` (nessun hard-delete applicativo del target: FK irraggiungibile dal flusso). Trigger: prossimo branch che tocca tipologie o pacchetti, o una decisione esplicita su erasure↔noleggi. Impatto se ignorata: basso — le guardie applicative coprono i flussi normali, resta la sola finestra read-committed.


- <a id="d-063"></a>**D-063** — **Permessi dello staff configurabili dall'admin del lido.** Oggi la corrispondenza
  permesso → ruoli è una tabella statica in `apps/api/src/identity/permission.ts`, uguale per tutti
  i lidi. L'admin deve poter decidere cosa il proprio staff può fare (modificare il listino, aprire
  campagne di rinnovo, gestire il catalogo noleggi…).
  **Il prerequisito è già fatto** ([ADR-0057](decisions/0057-autorizzazione-fail-closed-permessi.md),
  Fase C dell'audit): il guard è fail-closed e **tutti** gli endpoint dichiarano un permesso, quindi
  questa slice cambia *come il permesso viene risolto* — non richiede di riannotare ~60 rotte.
  **Il brief completo — punto di partenza, decisioni aperte, principi, gotcha verificati, findings
  correlati e come verificare — è in
  [2026-07-25-permessi-configurabili-design.md](../superpowers/specs/2026-07-25-permessi-configurabili-design.md).**
  Le tre decisioni che aprono la slice: per-lido o per-operatore; permessi nel JWT (stantii fino a
  8h, eredita D-026) o riletti a ogni richiesta; se e quando spostare `Permission` in
  `@coralyn/contracts` per far seguire al frontend ciò che il backend nega.
  *Trigger*: il primo lido che chiede una divisione dei compiti diversa da admin/staff.
  *Impatto se ignorata*: bassa — il default di fabbrica riproduce il comportamento attuale, quindi
  nessun lido regredisce; ma ogni richiesta di personalizzazione resta una modifica al codice.


> Nota: le voci sopra sono il punto di partenza emerso dal brainstorming iniziale e
> verranno raffinate man mano.

## Chiuse

Restano qui con la ragione per cui erano state rimandate: serve a capire *perché* una cosa è
stata rimandata, non solo che è stata fatta.

| ID | Tema | Perché rimandata | Cosa la sblocca / trigger | Impatto se ignorata |
|---|---|---|---|---|
| <a id="d-013"></a>D-013 | Sospensione / cessione / disdetta dell'abbonamento — **disdetta anticipata + rimborso IMPLEMENTATA** (sotto-slice 1/3; spec [2026-07-06-subscription-termination-refund-design.md](../superpowers/specs/2026-07-06-subscription-termination-refund-design.md), piano omonimo): `POST /bookings/:id/terminate` admin-only, tronca `endDate` + `terminatedAt` + `refundedAmount` (status resta `confirmed`, nessun nuovo enum), rimborso pro-rata **suggerito nel FE** e sovrascrivibile (server valida solo i bound), UI nella Scheda cliente. Fix collegato: la **card ombrellone (mappa) non offre più il void crudo** sugli abbonamenti (`MapView` gate su `type!=='subscription'` → rimanda alla Scheda cliente/disdetta), **FE-only** — il blocco backend del `cancel` è stato scartato perché rompeva l'annullo legittimo di rinnovi futuri (la mappa mostra solo abbonamenti attivi, quindi copre il caso pericoloso). **Nessun nuovo ADR** (additivo su [ADR-0011](decisions/0011-incasso-base-nel-core.md)). **Restano deferite:** cessione/subentro e sospensione temporanea (quest'ultima in sinergia con D-035 assenze comunicate). **Aggiornamento 2026-07-08 (sotto-slice sospensione, COMPLETA e MERGIATA):** la **fondazione** della sospensione temporanea — refactor dell'occupazione da colonne dirette su `Booking` a intervalli su un child table **`BookingCoverage`** (nuovo **[ADR-0046](decisions/0046-occupazione-a-intervalli-coverage.md)**) — è **implementata e MERGIATA su `main`** (`133cfc7`, spec [2026-07-08-booking-coverage-refactor-design.md](../superpowers/specs/2026-07-08-booking-coverage-refactor-design.md), piano [2026-07-08-booking-coverage-refactor.md](../superpowers/plans/2026-07-08-booking-coverage-refactor.md)): l'anti-overlap (`coverage_no_overlap`) vive ora su `BookingCoverage`, non più su `Booking` (il vecchio `booking_no_overlap` è stato rimosso); comportamento invariato, nessun cambio di contratto verso il FE. Questo è **solo il prerequisito strutturale**: la sospensione vera e propria (buco nell'occupazione + rimborso proporzionato) è la **Spec 2** — **spec SCRITTA** 2026-07-08 su branch `feat/subscription-suspension` (commit `db324d8`, [design](../superpowers/specs/2026-07-08-subscription-suspension-design.md)): `BookingSuspension` child, due modalità (chiusa `[S,R-1]` / aperta+riattiva) unificate da `endDate` nullable, rivendita nel buco, rimborso mirror-disdetta aggregato su `Booking.refundedAmount`. **IMPLEMENTATA e MERGIATA** (2026-07-08, piano [2026-07-08-subscription-suspension.md](../superpowers/plans/2026-07-08-subscription-suspension.md), 11 commit `7ecd841..2568229`): `POST /bookings/:id/suspend` (chiusa `[S,R-1]` / aperta `[S,…)`) + `POST /bookings/:id/reactivate` **admin-only**; carve degli intervalli su `BookingCoverage` (buco rivendibile, coda `[R,end]` riservata); nuova `BookingSuspension` (RLS FORCE, pura storia); `CustomerBookingDTO.suspensions[]` in projection; rimborso FE-suggerito (server valida i bound) aggregato su `Booking.refundedAmount`; FE Scheda cliente (card + `SuspendSubscriptionModal`/`ReactivateSubscriptionModal` + hooks). **Nessun nuovo ADR** (additivo su [ADR-0046](decisions/0046-occupazione-a-intervalli-coverage.md)); design docs aggiornati ([ADR-0009]: `data-model.md` ER, `flows.md` stati+carve, mockup). Baseline post-merge: api unit **213** · api e2e **264** · web-staff **332** · typecheck pulito. **Aggiornamento 2026-07-08 (CESSIONE/SUBENTRO — COMPLETA e MERGIATA, D-013 CHIUSA):** implementata e mergiata FF su `main` (`9bf2322`, spec [2026-07-08-subscription-cession-design.md](../superpowers/specs/2026-07-08-subscription-cession-design.md), piano [2026-07-08-subscription-cession.md](../superpowers/plans/2026-07-08-subscription-cession.md)): `POST /bookings/:id/transfer` **admin-only** cambia `Booking.customerId` A→B sulla **stessa** prenotazione (span/seniority/prelazione preservati; occupazione `BookingCoverage` **non** toccata — la cessione tocca il titolare, non l'occupazione); riconciliazione incasso a **movimento netto** su `amountCollected` (`− refundToPrevious + collectedFromNew`, clamp `[0,totalPrice]`, `paymentStatus` ricalcolato), `refundedAmount` **INTATTO** (la cessione è un trasferimento, non una perdita → nuovo **[ADR-0047](decisions/0047-cessione-subentro-titolarita-incasso.md)**); nuova child **`BookingTransfer`** (RLS ENABLE+FORCE + policy `tenant_isolation`, pura storia, no `createdBy`→D-047); `GET /customers/:id/ceded-subscriptions` (staff) + `CustomerBookingDTO.transfers[]`; FE modale `TransferSubscriptionModal` + card "Cedi" + sezione "cessioni effettuate". Whole-branch opus **Ready-to-merge** (0 Crit/0 Imp), **LIVE-verificata** su Docker (happy path + 5 guardie, `refundedAmount` invariato live). Baseline post: api unit 223 · api e2e 273 · web-staff 348. **D-013 interamente CHIUSA** (disdetta + sospensione + cessione + fondazione coverage). | Esigenza concreta (subentri, rimborsi, sospensioni). | ~~Bassa~~ **CHIUSA**: disdetta + sospensione + cessione + coverage tutte FATTE. |
| <a id="d-035"></a>D-035 | **Servizio clienti parallelo al gestionale + "assenze comunicate" degli abbonati** (canale rivolto al cliente del lido, distinto dal gestionale operatore) | **Motivazione di dominio (utente 2026-07-04):** in un lido la maggioranza si **abbona**, quindi la "presenza" NON è catturabile dall'operatore (appello a 200-300 persone = utopico) né deducibile (l'abbonato non avvisa se salta un giorno). Il bottone «Presenza» sulla mappa è stato **rimosso** proprio per questo. Il dato può esistere solo se lo fornisce **il cliente stesso**, tramite un canale suo che oggi non esiste. **Modello proposto dall'utente:** (1) alla creazione dell'**abbonamento** l'operatore chiede al cliente se accetta una **clausola "assenze comunicate"** (consenso esplicito, spuntabile); (2) se la clausola è accettata, il cliente potrà segnalare **dal proprio dispositivo** (telefono/altro) di essere **SICURO di NON essere presente** in una specifica **fascia di uno specifico giorno** compreso nel suo abbonamento — **anche con anticipo** (non solo lo stesso giorno per lo stesso giorno); (3) **solo** a fronte di quella segnalazione esplicita l'operatore può prenotare quell'ombrellone, per quella fascia e quel giorno, ad altri. **Invariante non negoziabile:** in assenza di segnalazione esplicita dal sistema, **anche se il cliente di fatto non si presenta, l'operatore NON può rivendere** quelle fasce a nessuno (nessuna presunzione d'assenza). Sblocca la "rivendita del posto liberato" senza dover fare presenze a mano. **Decomposizione concordata (2026-07-09):** il modulo è stato scomposto in **S1→S2** (lato operatore) e **S3→S4** (canale cliente), valore/rischio prima, canale client per ultimo. **Aggiornamento 2026-07-09 (S1+S2 — consenso + release operatore + rivendita — IMPLEMENTATE e poi MERGIATE su `main`):** spec [2026-07-09-assenze-comunicate-release-operatore-design.md](../superpowers/specs/2026-07-09-assenze-comunicate-release-operatore-design.md), piano [2026-07-09-assenze-comunicate-release-operatore.md](../superpowers/plans/2026-07-09-assenze-comunicate-release-operatore.md), nuovo **[ADR-0048](decisions/0048-assenze-comunicate-release-occupazione.md)**: `Booking.absenceConsentAt` (stato corrente del consenso, `PATCH /bookings/:id/absence-consent` admin-only idempotente) + nuova child **`AbsenceRelease`** (RLS ENABLE+FORCE, pura storia, mirror `BookingSuspension`/`BookingTransfer`, `source` (`operator`/`customer`) predisposto per S4) — `POST /bookings/:id/absence-releases` (carve **giorno-singolo** su `BookingCoverage`, consenso-gated: `422 NO_CONSENT` senza consenso attivo; `422 BAD_DATE`/`PAST_DATE`; `409 ALREADY_RELEASED`; `422 NO_COVERAGE`) + `POST /bookings/:id/absence-releases/:rid/cancel` (ri-copre se non rivenduto, altrimenti `409 RESOLD` — vincolante), tutti admin-only; `Booking.amountCollected`/`refundedAmount` **invariati** (ADR-0048: compensazione = rinuncia al diritto, non mancato utilizzo — zero cassa sull'abbonato, a differenza di disdetta/sospensione); rivendita = prenotazione giornaliera indipendente sul flusso esistente, nessun endpoint dedicato; `CustomerBookingDTO.absenceConsentAt`/`.absenceReleases[]` (con `resold` derivato in projection); FE Scheda cliente (`CustomerSubscriptionsCard` azioni consenso/segnala assenza + sezione release, `AbsenceReleaseModal`, hooks con invalidazione della Scheda cliente). Design docs aggiornati (ADR-0009): `data-model.md` ER, `flows.md` §7, mockup `absence-release-modal.html`. **Restano deferite S3 (auth cliente self-service: [D-026](deferred.md)/[D-027](deferred.md)/[D-028](deferred.md)/[D-029](deferred.md)) e S4 (canale PWA/QR + gestione `401` FE, [D-037](deferred.md))**: portano il tenant-routing pubblico e l'auth cliente, fuori scope di questa sotto-slice; `AbsenceRelease.source='customer'` è già predisposto senza retrofit. **D-035 NON è chiusa** (solo S1+S2 fatte). **Aggiornamento 2026-07-15 (S3 — fondazione auth canale cliente, IMPLEMENTATA e MERGIATA FF in `main` locale, branch `feat/customer-channel-d035-s3` eliminato; `main` locale avanti su `origin/main`, NON ancora pushata):** spec [2026-07-10-canale-cliente-self-service-d035-s3-s4-design.md](../superpowers/specs/2026-07-10-canale-cliente-self-service-d035-s3-s4-design.md), piano [2026-07-10-canale-cliente-auth-d035-s3.md](../superpowers/plans/2026-07-10-canale-cliente-auth-d035-s3.md), nuovo **[ADR-0049](decisions/0049-auth-cliente-provisioned-tenant-pubblico.md)**: accesso **provisioned dall'operatore** (`POST /bookings/:id/customer-access` admin-only genera enrollment token opaco + PIN, solo-hash) → attivazione one-time+PIN (`POST /customer/activate`, lock a soglia, 401 generico) → **access JWT cliente** breve (`kind:'customer'`) + **refresh device-bound rotante** (`POST /customer/refresh` con **theft-detection**: riuso ⇒ revoca catena) + `POST /customer/logout` + `GET /customer/me` (`CustomerJwtGuard`); due tabelle **fuori-RLS** (`CustomerEnrollmentToken`/`CustomerSession`) con `establishmentId` denormalizzato come **sorgente del tenant** (guard dedicato popola `req.tenantId`, `forTenant`/RLS invariati); rate-limit **controller-scoped** su `/customer/*` (`@nestjs/throttler`). **Risolve [D-026](deferred.md)/[D-027](deferred.md)/[D-029](deferred.md)** (vedi Risolte); **D-028** valutato e confermato non-trigger. Design docs aggiornati (ADR-0009): `data-model.md` ER (2 entità + invariante), `flows.md` §9 (macchina a stati accesso + rotazione). **Resta S4** (feature release `source='customer'` + app `web-customer` + gestione 401 FE [D-037](deferred.md)); il **merge-gate** di S3 è passato (FF in main locale) — manca solo il **push** su `origin/main` (e l'eventuale review whole-branch opus, come le slice precedenti). **Aggiornamento 2026-07-15 (S4 — canale cliente FE, IMPLEMENTATA):** spec/piano [2026-07-15-canale-cliente-d035-s4.md](../superpowers/plans/2026-07-15-canale-cliente-d035-s4.md), addendum su **[ADR-0049](decisions/0049-auth-cliente-provisioned-tenant-pubblico.md)**: tre endpoint cliente (`GET /customer/me/subscriptions`, `POST /customer/subscriptions/:bookingId/absence-releases[/:rid/cancel]`, `CustomerBookingsController` guarded `CustomerJwtGuard`) che **riusano** senza duplicazione i domain service `releaseAbsence`/`cancelAbsenceRelease` di S1/S2, estesi con `source` (impostato dal **controller**, non dal body — refinement di sicurezza sulla spec) + ownership `actingCustomerId` (mismatch → 404); nuova app **PWA `web-customer`** (clone di `web-platform`, chiavi `localStorage` distinte, viste attivazione/abbonamenti /segnala-assenza/storico) con **interceptor `401` → refresh single-flight** (D-037 — è in tabella, non fra le Risolte: riaperta il 2026-07-25 per `web-platform`); **nessuna nuova tabella/migration** (schema invariato da S1/S2/S3). Design docs aggiornati (ADR-0009): ADR-0049 addendum, `flows.md` §7 (nota `source='customer'`), 4 mockup `web-customer-*.html`. **D-035 è ora CHIUSA** (S1+S2+S3+S4 tutte fatte e mergiate). Baseline post: api e2e **353** · web-customer **21** (nuova). | ~~S4: decisione di costruire il **canale cliente FE**~~ **FATTO** (vedi aggiornamento 2026-07-15). | ~~Media~~ **CHIUSA**: S1+S2+S3+S4 tutte fatte, nessun retrofit distruttivo — `source`/auth/app cliente completi. |
| <a id="d-037"></a>**D-037 — ✅ CHIUSA 2026-07-25 (Fase F), dopo essere stata RIAPERTA lo stesso giorno per `web-platform`**. Gestione globale del `401` nel data-layer FE (interceptor unico → `session.logout()` + redirect a `/login` su token scaduto/invalido, invece del banner d'errore per-view) | **Osservato 2026-07-04** durante la verifica dello Stabilimento: con un token JWT scaduto (durata 8h) e **navigazione client-side**, ogni view mostra il proprio **stato d'errore** invece di rimandare al login, perché `rehydrate()`/`GET /auth/me` valida la sessione **solo al reload completo** della pagina, non sui cambi di rotta interni. `apiFetch` ([`http.ts`](../../apps/web-staff/src/lib/http.ts)) propaga il `401` come errore di query senza intercettarlo globalmente. Non è un difetto di una singola schermata: è **app-wide** e pre-esistente. ~~Lega a [D-026](deferred.md) (refresh & revoca token)~~ **Aggiornamento 2026-07-15:** D-026 è ora **risolta** per il canale cliente ([ADR-0049](decisions/0049-auth-cliente-provisioned-tenant-pubblico.md)); D-037 resta distinto e aperto — qui basta gestire *gracefully* qualunque `401` (senza refresh) in **web-staff**, e la **stessa** gestione globale del 401 servirà alla futura app **`web-customer`** (D-035 S4, dove il refresh cliente esiste e un 401 può innescare rotazione o re-login). **Aggiornamento 2026-07-15 (D-035 S4 — RISOLTA per `web-customer`):** `src/lib/http.ts` implementa l'interceptor globale — un `401` avvia (o si accoda a, single-flight) un `POST /customer/refresh`, poi riprova la richiesta col nuovo access token; se il refresh stesso fallisce, `session.logout()` + redirect a `/attiva` (D-026, [ADR-0049](decisions/0049-auth-cliente-provisioned-tenant-pubblico.md) addendum S4). ⛔ **Correzione 2026-07-25 (Fase D):** il «+ redirect a `/attiva`» di questa riga era **falso quando è stato scritto** — `onAuthFailure` era `logout()`, che azzerava lo stato e **non navigava**; il guard del router scatta solo sulle navigazioni, quindi il bagnante restava su «I miei abbonamenti» con un `EmptyState` (AUD-010/P4-001). È **vero da adesso**: il redirect vive in `CustomerShell`, che osserva `authenticated` ed esclude le rotte `meta.public`. Gli endpoint `customer-auth` sono `retryOn401:false` (evita ricorsione sul refresh). **Lo stesso pattern resta applicabile a `web-staff` come follow-up**: `web-staff` **non** è toccato da questa slice e conserva il comportamento per-view osservato nel 2026-07-04 — D-037 è chiusa solo per il canale cliente, non per lo staff. **Aggiornamento 2026-07-21 (web-staff — RISOLTA per `web-staff`; la riga diceva «D-037 ora interamente CHIUSA», ed era falso: vedi la colonna Priorità):** scelto l'`onError` globale del `QueryClient` (**non** un interceptor in `http.ts`, che è volutamente disaccoppiato da Pinia/router — cfr. commento in `authToken.ts`); logica pura in [`onApiError.ts`](../../packages/data-layer/src/onApiError.ts) (`handleUnauthorized`), agganciata a `QueryCache`/`MutationCache` in [`queryClient.ts`](../../apps/web-staff/src/lib/queryClient.ts). Agisce **solo se la sessione è attiva** (`session.authenticated`): così un 401 da login errato (non ancora autenticato) o da `rehydrate` di un token scaduto resta gestito localmente, e non c'è redirect-loop sulla rotta `login`. Preserva la rotta in `?redirect`, che `LoginView` ora onora **solo per path interni** (guardia anti-open-redirect: no `//host`/URL assoluti). web-staff non ha refresh (D-026 non esteso allo staff) → solo logout+redirect, nessuna rotazione. Nessun nuovo ADR (additivo, error-surface FE). | ~~Sessioni reali che superano gli 8h del JWT, esposizione pubblica, o richiesta di UX di re-login pulita~~ **FATTO** (web-customer 2026-07-15 + web-staff 2026-07-21). | ✅ **CHIUSA su tutte e tre.** ⚠️ **Aggiornato 2026-07-25 ([ADR-0058](decisions/0058-package-data-layer-condiviso.md), D-065):** questa riga diceva «gemello VOLONTARIO di quello di web-staff, non un'astrazione condivisa — le due app hanno ciascuna il proprio `ApiError`, e unificarle e' una decisione strutturale». Era **vero quando è stato scritto** ed è **cessato di esserlo** lo stesso giorno: la decisione strutturale è stata presa. `handleUnauthorized` esiste ora in **una sola copia**, in [`@coralyn/data-layer`](../../packages/data-layer/src/onApiError.ts), e i 6 test che la vincolano — che erano in due copie identiche, una per app — valgono per entrambe. Il presupposto tecnico era proprio `ApiError`: unica per tutto il monorepo, altrimenti `instanceof` non attraversa il confine. `apps/web-staff/src/lib/onApiError.ts` e `apps/web-platform/src/lib/onApiError.ts` **non esistono più**; le due app compongono il proprio `queryClient` con `createQueryClient({ getSession, router })`. Storico: era **APERTA su `web-platform`**, chiusa sulle altre due (a UX, non a rischio dati — il `401` protegge comunque l'endpoint). ⛔ **Verificato 2026-07-25 (AUD-014):** `apps/web-platform/src/lib/queryClient.ts` non ha **alcun** `onError`, non esiste un `onApiError.ts` in quell'app, e **`web-platform` non è mai stata nominata in nessuno dei tre aggiornamenti** che hanno portato la voce a «CHIUSA». Una deferred chiusa per sbaglio è peggio di una aperta: nessuno rilegge le voci chiuse. ~~Il pattern da copiare è `web-staff/src/lib/onApiError.ts` + l'aggancio in `queryClient.ts`.~~ — istruzione **superata il 2026-07-25**: non c'è più un pattern da copiare, c'è [`createQueryClient`](../../packages/data-layer/src/queryClient.ts) da chiamare (ADR-0058). Copiare era esattamente il problema. |
| <a id="d-039"></a>~~D-039~~ **RISOLTA 2026-07-04** — Drift Prisma su `Rate_signature_key` | Risolta: aggiunto `@@unique([...], map: "Rate_signature_key")` in `schema.prisma` per le stesse 8 colonne della firma. Prisma diventa **consapevole** dell'indice ed è cieco al modificatore `NULLS NOT DISTINCT` → `migrate diff`/`migrate dev` vedono schema e DB allineati e **non** generano più il `DROP INDEX` spurio (verificato: *empty migration*, anche via shadow DB). Nessuna migrazione: l'indice esiste già; il modificatore resta nella migrazione raw `20260630203447_pricing`, autoritativa. ⚠️ Non usare `prisma db push`. Vedi [ADR-0032](decisions/0032-pricing-engine-precedenza.md) §3. | — | — | ## Risolte
| <a id="d-045"></a>D-045 | **Rimuovere lo stub console superuser da `web-staff`** (ora rimpiazzato da `apps/web-platform`) + `web-staff` login rifiuta i superuser | Con la Platform Console realizzata come **app dedicata `web-platform`** ([ADR-0041](decisions/0041-app-frontend-dedicata-platform.md)), la rotta `/console` di `web-staff` (`features/console/ConsoleView.vue`, badge "Console superuser · in arrivo", `meta.role: Superuser`) è **obsoleta**: un superuser non usa più il gestionale-lido. Inoltre `web-staff` login **non** rifiuta un superuser (asimmetrico rispetto a `web-platform` che rifiuta i non-superuser), ma un superuser ha `establishmentId=null` → le API tenant-scoped darebbero 400: sessione di fatto rotta. Cleanup: rimuovere `ConsoleView.vue` + la rotta `/console`, e far rifiutare a `web-staff` il login dei superuser (specularmente a `web-platform`, riusando il pattern di `apps/web-platform/src/stores/session.ts`). **Aggiornamento 2026-07-06 (parziale, commit `6feebf3`):** lo **stub è stato RIMOSSO** — `ConsoleView.vue` + rotta `/console` + link sidebar eliminati. **CHIUSO 2026-07-06 (commit `edbf86a`):** implementata la **guardia di login** — `web-staff` `login` fa throw (prima di `setToken`) se `role === Superuser` e `rehydrate` fa `logout` se `/me` è superuser, specularmente a `web-platform`; +2 test store, +1 spec Sidebar. **D-045 COMPLETO.** NB: le altre "· in arrivo" in `EstablishmentView.vue` sono fallback per non-admin o sotto-feature legittime (inviti staff = slice email), non obsolete. | Coerenza post-ADR-0041; un superuser che prova web-staff. | ~~Bassa~~ **FATTO**. |
| <a id="d-051"></a>D-051 | **RISOLTA 2026-07-15** — UI operatore per il provisioning dell'accesso cliente in `web-staff` (azione «Genera accesso cliente» + «Revoca» + stato accesso nella Scheda cliente) | Il **backend esiste** dal D-035 S3 ([ADR-0049](decisions/0049-auth-cliente-provisioned-tenant-pubblico.md)): `POST /bookings/:id/customer-access` (admin-only → `{ activationUrl, pin, expiresAt }`, mostrati una volta) e `POST /bookings/:id/customer-access/revoke`. Ma **nessuna UI in `web-staff` li chiama** (grep 2026-07-15: 0 riferimenti): S3 = fondazione auth backend, S4 = canale cliente FE + endpoint cliente — la UI operatore di provisioning (spec S3+S4 §6.1) non era nello scope di nessuna delle due. Oggi l'operatore può generare/revocare l'accesso **solo via API** (curl). **Risolta:** nuovo `GET /bookings/:id/customer-access` (admin-only, `CustomerAccessStatusDTO`, tenant-scoped via `resolveCustomerId` sotto RLS → no IDOR; `accessStatus` reso **privato** nel service) + `CustomerAccessCard`/`CustomerAccessModal` nella Scheda cliente `web-staff` (badge stato `none/issued/active/revoked`, azioni Genera/Rigenera/Revoca, reveal QR+link+PIN una-volta con copia), 3 hook TanStack Query, nuova dipendenza `qrcode`, icone `copy`/`smartphone` in `ui-kit`. | Primo uso reale: un operatore deve poter consegnare link+PIN (e QR) al cliente **dall'interfaccia**, senza toccare l'API. | ~~Media~~ **FATTO**: il canale cliente è ora azionabile da un operatore reale, senza API dirette. |

### Voci estese

- <a id="d-001"></a>**D-001** — Stack tecnologico → risolta da [ADR-0008](decisions/0008-stack-e-layout.md).

- <a id="d-011"></a>**D-011** — Prelazione abbonamenti → **implementata** (branch `feat/d011-prelazione`, spec [2026-07-01-prelazione-abbonamenti-d011-design.md](../specs/2026-07-01-prelazione-abbonamenti-d011-design.md), piano [2026-07-02-prelazione-abbonamenti-d011.md](../superpowers/plans/2026-07-02-prelazione-abbonamenti-d011.md)). Nuovo **[ADR-0034](decisions/0034-prelazione-finestre-lazy.md)** (finestre derivate lazy; `RenewalCampaign` unico stato persistito; hold applicativo in `priceAndWrite`; nessun `BookingStatus` nuovo). Test: api unit 77→83, e2e 90→110; web-staff 93→100; ui-kit 41 (invariato).

- <a id="d-017"></a>**D-017** — Brand pubblico e dominio definitivo → risolta da [ADR-0029](decisions/0029-brand-coralyn.md): brand **Coralyn**, scope pacchetti `@coralyn/*`, identificatori infra `coralyn_*`. Il codename provvisorio *Driftly* è dismesso.

- <a id="d-022"></a>**D-022** — Validazione server-side dell'input API → risolta da [ADR-0023](decisions/0023-contatti-cliente-colonne-tipizzate.md): DTO `class-validator` (`CreateClienteDto`/`UpdateClienteDto`) + `ValidationPipe({ whitelist, transform })` globale in `main.ts`. Email malformata → 400; campi non dichiarati scartati. Coperta da e2e in `apps/api/test/clienti.e2e-spec.ts`.

- <a id="d-026"></a>**D-026** — Refresh & revoca dei token → **risolta per il canale cliente** (D-035 S3, [ADR-0049](decisions/0049-auth-cliente-provisioned-tenant-pubblico.md)): `CustomerSession` è un **refresh token device-bound rotante** (`POST /customer/refresh`) con **theft-detection** (riuso di un refresh già ruotato ⇒ revoca dell'intera catena della sessione) e **revoca** operatore (`POST /bookings/:id/customer-access/revoke`) + logout (`POST /customer/logout`). Il **login staff** resta un access JWT stateless a vita breve **senza** refresh: accettabile perché non esposto pubblicamente (la superficie che rendeva D-026 rilevante è il canale cliente); se lo staff verrà esposto, si riapre come slice additiva sullo stesso pattern.

- <a id="d-027"></a>**D-027** — Rate-limiting / brute-force → **risolta per il canale cliente** (D-035 S3, [ADR-0049](decisions/0049-auth-cliente-provisioned-tenant-pubblico.md)): `@nestjs/throttler` **controller-scoped** su `/customer/*` (limite env `CUSTOMER_THROTTLE_LIMIT`/60s, 429 oltre soglia), **non** un `APP_GUARD` globale (che throttlerebbe lo staff interno e romperebbe la suite e2e). Il login staff interno resta senza throttling (non esposto); riapribile additivamente all'esposizione. → **CHIUSA anche per il canale staff (2026-07-25, Fase C dell'audit).** La premessa «non esposto» era falsa dal 17/07: la slice deploy aveva messo il login su Internet dietro Caddy con TLS. `ThrottlerGuard` è ora applicato **per metodo** alle 3 rotte `@Public` di `AuthController` (login, contesto e riscatto del link set-password), limite `STAFF_AUTH_THROTTLE_LIMIT`/60s (20 predefinito) con contatore separato per rotta. **Non** di classe: `GET /auth/me` è chiamata a ogni caricamento dell'app e nello stesso bucket diventerebbe il primo 429 della giornata. **Non** come definizione nominata aggiuntiva: in throttler v6 il guard valuta tutte le definizioni configurate, quindi una seconda avrebbe stretto anche il canale cliente — è una sovrascrittura per-rotta di quella esistente. Prerequisito indispensabile: `trust proxy` (vedi ADR-0057 e `configure-app.ts`), senza il quale `req.ip` era identico per ogni richiesta passata dal proxy e il limite era un bucket globale. Test: `staff-auth-hardening.e2e-spec.ts`. **Resta aperto**: la chiave è l'IP, quindi un lido dietro NAT condivide il contatore fra tutti i suoi operatori; una chiave per identità è una proposta non implementata.

- <a id="d-029"></a>**D-029** — Anti-enumeration / timing-oracle → **risolta per il canale cliente** (D-035 S3, [ADR-0049](decisions/0049-auth-cliente-provisioned-tenant-pubblico.md)): ogni fallimento di `activate`/`refresh` è un **401 generico identico** (token inesistente/scaduto/revocato/già-attivato, PIN errato) — nessun ramo osservabile dall'esterno, per costruzione (non un `verify` civetta). Il login staff interno conserva il comportamento originale (accettabile: non esposto). → **CHIUSA anche per il canale staff (2026-07-25, Fase C dell'audit).** Stessa premessa caduta di D-027. `identity.service.login` esegue ora una verifica argon2 contro un **hash civetta** quando l'email non esiste (`PasswordHasher.verifyDecoy`, hash generato da un valore casuale e condiviso dal processo): il ramo «utente inesistente» costa quanto «password errata», quindi il tempo di risposta non distingue più un'email registrata da una no. L'ordine importa: la civetta **senza** rate-limit avrebbe trasformato la mitigazione in un vettore di esaurimento CPU, per questo è arrivata insieme a D-027 e non prima. Test: i due rami di `login` in `identity.service.spec.ts`, che il fake precedente (`verify` cablato su `true`) rendeva irraggiungibili.

**Baseline finale slice canale cliente auth D-035 S3 (2026-07-15):** api unit **238** (232 error-surface + 6 dai task S3 T3/T4/T5 già committati — `CustomerTokenService` 2, `generatePin` 1, `CustomerJwtGuard` 3; i task T7-T11 aggiungono solo e2e) · api e2e **342 passati** (330 + 11 nuovi: activate 3, refresh 2, me+logout 3, isolation 2, throttle 1) · typecheck `tsc -p tsconfig.json --noEmit` **pulito**. ⚠️ **3 pre-esistenti fallimenti date-dipendenti** in `bookings.e2e-spec.ts` (`macchina a stati CTA`, release su `2026-07-10` ora nel passato → `422 PAST_DATE`): **non** una regressione S3 (file bookings identici a pre-slice), tracciati a parte per il fix (rendere le date relative a `todayInRome()`).


- <a id="d-030"></a>**D-030** — Exclusion constraint DB per l'anti-overlap → **implementato** (branch `feat/d030-anti-overlap-db`, spec [2026-07-03-anti-overlap-db-d030-design.md](../specs/2026-07-03-anti-overlap-db-d030-design.md), piano [2026-07-03-anti-overlap-db-d030.md](../superpowers/plans/2026-07-03-anti-overlap-db-d030.md)). Nuovo **[ADR-0037](decisions/0037-anti-overlap-exclusion-constraint.md)** (raffina [ADR-0006](decisions/0006-dominio-prenotazioni-e-pricing.md)/[ADR-0013](decisions/0013-granularita-disponibilita-a-slot.md)). `EXCLUDE USING gist` (`btree_gist`) su `umbrellaId =` + `daterange('[]')` + `int4range('[)')` dei minuti-fascia denormalizzati su `Booking` (DB-autoritativi via trigger `booking_fill_slot_minutes_trg`), `WHERE status='confirmed'`, semantica allineata a `dateRangesOverlap`/`slotsOverlap`; violazione `23P01→409` (stesso messaggio del controllo applicativo, che resta primario); validazione stagioni rinnovo rafforzata (`dest.startDate > origin.endDate`) enforcata in DUE punti (`renewal-campaigns.open()` + `bookings.renew()` pre-flight 422) → constraint rinnovo-safe e backstop di sola race. Test: api unit 97→101, e2e 142→153.

- <a id="d-032"></a>**D-032** — Editor CRUD del listino → **implementato** (branch `feat/d032-pricing-editor`, spec [2026-07-01-pricing-editor-d032-design.md](../specs/2026-07-01-pricing-editor-d032-design.md), piano [2026-07-01-pricing-editor-d032.md](../superpowers/plans/2026-07-01-pricing-editor-d032.md)). **Nessun nuovo ADR** (incremento CRUD tenant-scoped sull'architettura già decisa da [ADR-0032](decisions/0032-pricing-engine-precedenza.md), pattern `Customers`). `SeasonsController`/`RatesController` + scritture `PackagesController` in `CatalogModule`; `Pricing` 1:1 automatico e mai esposto; delete stagione a cascata applicativa; non-ambiguità `Rate` (indice raw `Rate_signature_key`, 23505→409) senza migrazione; delete pacchetto referenziato → 409 (FK `packageId` sono `ON DELETE SET NULL`). FE: `PricingView` da mock a editor reale. Test: api unit 68→77, e2e 73→90; web-staff 83→93. `pricing.engine.ts` invariato.

- <a id="d-041"></a>**D-041 + D-050** — Filtro globale Prisma `P2002`→`409` (D-041) + `P2023`→`400` id malformato (D-050) → **risolte insieme** (branch `feat/error-surface-d041-d050-d049`, piano [2026-07-10-error-surface-d041-d050-d049.md](../superpowers/plans/2026-07-10-error-surface-d041-d050-d049.md)) da un unico `PrismaExceptionFilter` (`apps/api/src/common/prisma-exception.filter.ts`): mapper puro `mapPrismaKnownError(code)` (`P2002`→409 "Operazione in conflitto…", `P2023`→400 "Identificatore non valido.", altrimenti `null` = delega) + classe che **estende `BaseExceptionFilter`** e per i codici non mappati chiama `super.catch` (500 + log **invariati**). Registrato via **`APP_FILTER`** in `AppModule` (non `useGlobalFilters` in `main.ts`): attivo in prod **e in ogni e2e per costruzione** (le suite importano `AppModule`) → zero drift test-vs-prod, nessuna dimenticanza di wiring nei test. Le difese locali restano **prima linea** con messaggi specifici: `ParseUUIDPipe` sui `:id` struttura (§4.1) e `mapConflict`/check-proattivi 409 per-service; il filtro è il backstop uniforme, anche sul catalog (`equipment-types`/`packages`/`rates`/`seasons`/`time-slots`) senza spargere `ParseUUIDPipe` lì (deferito consapevolmente: la via pigra). Copertura: 3 unit test del mapper + 13 e2e nuove (`catalog-error-surface.e2e-spec.ts`, id non-UUID sui 5 controller catalog → 400, prima 500). Nel farlo, estratto l'helper `apps/api/test/helpers/create-test-app.ts` (prefix `api` + `ValidationPipe` + `init`) ed eliminata la duplicazione del bootstrap e2e in 22 suite (24 blocchi `beforeAll`); 3 suite pipe-less lasciate invariate (2 non bootstrappano un'app Nest, 1 — `booking-overlap-constraint` — con commento esplicativo). **Nessun nuovo ADR** (error-surface, [ADR-0009] N/A). **Deferito a triage** (Minor M1): `error` nella risposta deriva da un ternario sullo status invece che dal mapper — mislabeling latente se si aggiunge un 3° codice mappato; fix cheap = ritornare `error` da `mapPrismaKnownError`.

- <a id="d-048"></a>**D-048** — Fasce orarie sovrapposte / "Giornata intera" come **prodotto reale** + stato mappa **`covered`** onesto → **implementata** (branch `feat/timeslots-map` mergiato su `main`, spec [2026-07-06-timeslots-overlap-design.md](../superpowers/specs/2026-07-06-timeslots-overlap-design.md), piano [2026-07-06-timeslots-overlap.md](../superpowers/plans/2026-07-06-timeslots-overlap.md)). **Nessun nuovo ADR** (additivo su [ADR-0020](decisions/0020-resa-mappa.md)/[ADR-0037](decisions/0037-anti-overlap-exclusion-constraint.md)). Origine: una fascia sovrapposta senza prenotazione propria appariva come **prenotata fantasma** ("Giornaliero" senza cliente/importo). Fix: `SlotState` += `'covered'`; `UmbrellaDTO.coveredBySlot?`; `map.projection` a **due fasi** (booking diretta `timeSlotId===slot.id` vs **coperta** per overlap con altra fascia — overlap calcolato **solo backend**, il FE legge `coveredBySlot`); i **report** escludono le coperte da occupazione/`stateMix` (`occupancyStates`, niente doppio conteggio); FE rende **"Non disponibile"** + copritori ("coperta da {fascia} — {cliente} · €{importo}"), coperta **non prenotabile** (coerente col vincolo DB `booking_no_overlap` di [ADR-0037](decisions/0037-anti-overlap-exclusion-constraint.md)). Simmetrico full-day↔metà. Prerequisito: la slice **N-fasce** (mappa a N spicchi, drawer N box, messaggio computato) nello stesso branch. Test: ui-kit 73→79, web-staff 257→268, api unit 200→205 (e2e 235 invariato); tutte le slice whole-branch-reviewed su opus (0 Crit/0 Imp). **Resta deferito** (spec §7): ridefinizione formale dell'**occupancy%** sotto slot sovrapposte (l'esclusione delle coperte rimuove il doppio conteggio principale, ma il denominatore = totale slot-istanze resta influenzato — lega a [D-036](deferred.md)) e gli **hint di sovrapposizione nell'editor Struttura**. NB: colore token `--color-state-covered` (#BEB6A8) **rivisto e accettato** (2026-07-06); il flag di vicinanza a `--color-state-normal-mark` (#D8CDBB) è chiuso (righe distinte in legenda).

- <a id="d-049"></a>**D-049** — `testTimeout` esplicito nella config e2e → **risolta** (stesso branch/piano di D-041/D-050, primo task): `"testTimeout": 20000` aggiunto a `apps/api/test/jest-e2e.json`; il full-run `corepack pnpm test:e2e --runInBand` (**senza** `--testTimeout` manuale) è ora stabile per default — i tempi reali di app-init restano < 5s a macchina scarica, quindi non maschera regressioni di performance dei test.

**Baseline finale slice error-surface (2026-07-10):** api unit **232** (229+3 mapper) · api e2e **330** (317+13 catalog-error-surface) · web-staff **375** (non toccato) · typecheck `tsc -p tsconfig.json --noEmit` pulito.


- <a id="d-055"></a>**D-055** — **«Ritira ombrellone» (soft-delete)**: un ombrellone con storico prenotazioni (anche solo scadute/cancellate — le «prenotazioni fantasma») **non è eliminabile by design** (guardia block-409 che conta tutte le Booking + FK `Booking.umbrellaId` RESTRICT, [ADR-0052](decisions/0052-editor-struttura-cantiere.md)); la disdetta anticipata libera il posto in mappa ma non sblocca l'eliminazione. Gap dimostrato sul campo (2026-07-22): oggi l'unica via è distruggere storico contabile. Proposta: colonna `retiredAt` su `Umbrella` (sparisce da struttura/mappa/generatore, storico intatto); da decidere in brainstorming: unicità della label per i ritirati, proiezioni, UI nel pannello Ombrellone del Cantiere. → **risolta** (2026-07-23, branch `feat/ritira-ombrellone-d055`, [ADR-0053](decisions/0053-ritiro-ombrellone-soft-delete.md), [spec](../superpowers/specs/2026-07-22-ritira-ombrellone-d055-design.md)): `retiredAt` + **sgancio dalla fila** (`rowId` nullable, `onDelete: Restrict`) + snapshot `retiredFrom`; unicità label **solo tra gli attivi** via indice unico parziale (`WHERE "retiredAt" IS NULL`); endpoint admin idempotenti `retire`/`restore`/`retired` (guardia 409 sulle prenotazioni confermate con `endDate >= oggi`); nel Cantiere «Ritira» nel pannello Ombrellone e sezione «Ritirati» con Ripristina nel pannello Spiaggia. Follow-up in backlog: ~~wiring di `retiredFrom` nello storico prenotazioni~~ **FATTO** (2026-07-23, branch `feat/retiredfrom-storico-d055`, [spec](../superpowers/specs/2026-07-23-retiredfrom-storico-prenotazioni-design.md)): `CustomerBookingDTO` espone `umbrellaRetiredAt`/`umbrellaRetiredFrom` (additivi, valorizzati solo per i ritirati; `sectorName` invariato = settore vivo), `listByCustomer` li arricchisce senza query nuove, la Scheda cliente rende la posizione storica + badge «Ritirato» via l'helper condiviso `positionLabel` (che riduce il chip prima triplicato). Restano: reason dedicata `UMBRELLA_RETIRED` nel quote e [D-060](deferred.md) (risoluzione label nelle viste che usano `useEntityLabels`).


- <a id="d-056"></a>**D-056** — **MapView discrimina il settore «Speciali» per nome-stringa** (`s.name.toLowerCase()` ~L52) invece che per `Sector.kind` (che esiste a schema dal 07-04 ed è editabile dal Cantiere): un rename rompe la resa dedicata, e un settore `kind: special` con altro nome non la ottiene. Fix piccolo ma tocca la Mappa (fuori dallo scope del Cantiere). Nota dati correlata: nel seed dev «Speciali» è `kind: grid`. → **risolta** (2026-07-22, branch `fix/mappa-kind-tabpanel`): `SectorDTO` della mappa-giorno espone `kind` (additivo, proiettato dall'API), MapView rende come blocco dedicato in coda OGNI settore `kind: special` col suo nome reale; il seed dev allinea «Speciali» a `kind: special` (anche nell'update dell'upsert, per i DB esistenti).


- <a id="d-057"></a>**D-057** — **I tab settore del Cantiere non dichiarano un `tabpanel`**: `StructureScene.vue` usa `role="tablist"`/`role="tab"` (pre-esistenti, dal primo Cantiere) senza `aria-controls` né alcun `role="tabpanel"` sulla sabbia — gli screen reader annunciano tab che non controllano nulla. Il polish 2026-07-22 ha aggiunto la metà mancante del pattern APG (tablist puro coi soli tab, roving tabindex, frecce/Home/End ad attivazione automatica) ma non la relazione tab↔pannello, fuori dallo scope del chip. Due vie alla prossima occasione: dare un `id` alla regione sabbia + `aria-controls`/`role="tabpanel"`, **oppure** declassare i ruoli a una toolbar di bottoni se il modello mentale non è davvero «tab». Nota collegata: `StructureScene.spec.ts` monta con `attachTo: document.body` senza `enableAutoUnmount(afterEach)` (rischio basso: la scena non registra listener su `window`) — una riga da aggiungere quando il file si tocca. → **risolta** (2026-07-22, branch `fix/mappa-kind-tabpanel`) con la prima via: i tab commutano davvero il contenuto della sabbia, quindi il modello «tab» è corretto — `id`/`aria-controls` sui tab, sabbia = tabpanel unico riusato (`id` stabile `st-tabpanel`, `aria-labelledby` sul tab attivo, ruolo assente senza settori per non creare un tabpanel orfano). `enableAutoUnmount(afterEach)` aggiunto allo spec.


- <a id="d-058"></a>**D-058** — **Le FK opzionali di `Rate` sono `ON DELETE SET NULL`** (`Rate_rowId_fkey`, `Rate_sectorId_fkey`, migration `20260630203447_pricing`): è il default che Prisma emette per ogni relation opzionale senza `onDelete` esplicito — lo stesso meccanismo che nel branch D-055 aveva silenziosamente degradato `Umbrella_rowId_fkey` (preso in review finale, [ADR-0053](decisions/0053-ritiro-ombrellone-soft-delete.md)). Qui la conseguenza è più insidiosa di un errore: azzerare `rowId`/`sectorId` non rompe la `Rate`, la rende **più generica** — una tariffa specifica per fila diventa wildcard su quella dimensione e può cambiare i prezzi in silenzio, o collidere con un'altra sulla firma `@@unique` (`NULLS NOT DISTINCT`, [ADR-0032](decisions/0032-pricing-engine-precedenza.md)). **Mitigato a livello applicativo**: `rows.service.remove` e `sectors.service.remove` rifiutano (409) la cancellazione se esistono `Rate` che le referenziano, quindi la FK non è raggiungibile dal flusso normale; resta la finestra read-committed (creare una `Rate` mentre la fila/settore viene cancellato). Fix: `onDelete: Restrict` esplicito sulle quattro relation opzionali di `Rate` + micro-migration, come fatto per `Umbrella`. Da valutare insieme a un giro sulle altre relation opzionali dello schema. → **risolta** (2026-07-23, branch `fix/d058-rate-fk-restrict`): `onDelete: Restrict` esplicito su `sector`/`row`/`package`/`timeSlot` di `Rate` (commento-guardia nello schema) + migration `20260723062405_rate_fk_restrict` — solo le 4 FK, verificato su `--create-only` che non tocchi l'indice parziale di `Umbrella`; canary e2e DB-level `rate-fk-restrict.e2e-spec.ts` (35ª suite: delete raw delle 4 dimensioni → P2003 e `Rate` intatta, + controllo che rimossa la `Rate` il delete torna possibile). Le guardie 409 dei service restano la prima linea; la FK chiude la finestra read-committed. **Nessun nuovo ADR** (estende ad altre FK la decisione già motivata in [ADR-0053](decisions/0053-ritiro-ombrellone-soft-delete.md) Consequences). Il «giro sulle altre relation opzionali» è fatto: esito tracciato in D-059 qui sotto.


- <a id="d-060"></a>**D-060** — **`useEntityLabels().umbrellaLabel` non risolve gli ombrelloni ritirati.**
  [`apps/web-staff/src/lib/useEntityLabels.ts`](../../apps/web-staff/src/lib/useEntityLabels.ts) costruisce
  la mappa id→label attraversando la **day-map viva** (settori → file → ombrelloni): un ritirato non ha
  fila, quindi non c'è. Consumatori impattati: `BookingsView` (una prenotazione passata su ombrellone poi
  ritirato mostra «—» al posto della **label intera** — peggio del caso Scheda cliente, che ora è risolto)
  e `RenewalsView` (abbonamenti della stagione origine su un ombrellone ritirato a fine stagione).
  **È lo stesso identico problema già risolto in questo file per i pacchetti archiviati** — il commento in
  testa spiega che `packageName` usa `useAllPackages()` (archiviati inclusi) proprio perché è un percorso di
  **risoluzione storica**, non un selettore: la stessa regola non è mai stata estesa agli ombrelloni perché
  il ritiro non esisteva ancora. Fix naturale: dare a `umbrellaLabel` una fonte che includa i ritirati
  (`GET establishment/umbrellas/retired` esiste già, admin-only — da valutare per lo staff) e mostrare la
  label storica come fa la Scheda cliente. Scoperta dalla review finale di D-055-storico; fuori dallo scope
  dichiarato di quel branch (solo Scheda cliente). Trigger: prossimo lavoro su Prenotazioni o Rinnovi.
  Impatto se ignorata: bassa/media — non è perdita di dati (lo storico è integro), è una vista che mente
  con «—»; la guardia 409 del retire limita l'esposizione ai giorni passati.
  → **risolta** (2026-07-23, branch `fix/d060-label-ombrelloni-ritirati`,
  [spec](../superpowers/specs/2026-07-23-d060-label-ombrelloni-ritirati-design.md)): la regola di
  risoluzione storica di `packageName`/`useAllPackages` è estesa agli ombrelloni — `umbrellaLabel`
  fonde i ritirati da `GET /establishment/umbrellas/retired`, **aperto anche allo staff** (i `@Roles`
  scendono dalla classe ai singoli handler di `UmbrellasController`: mutazioni e CRUD restano
  admin-only; il DTO è pura struttura senza PII, e2e del 403 aggiornata deliberatamente); nuovo
  `retiredUmbrellaIds` alimenta il badge «Ritirato» nelle celle di `BookingsView` e `RenewalsView`
  (entrambe le tabelle), come nella Scheda cliente. Alternativa rigettata: label in projection sui
  DTO di lista (seconda fonte di verità). Nessuna migration, nessun contratto nuovo.


- <a id="d-064"></a>**D-064** — **`GET /establishment/overview` esponeva `team[]` (email di tutti gli
  operatori) a chi ha `establishment.read`, cioè anche allo staff.**
  ✅ **CHIUSA il 2026-07-26** — [ADR-0060](decisions/0060-read-model-shell-senza-pii.md): `team[]`
  esce dall'overview, il team è `GET /establishment/users` sotto `team.manage` (il controller aveva
  già quel permesso di classe e POST/PATCH: mancava solo il GET).
  *Perché era stata rimandata*: è un **cambio di contratto FE/BE** (`EstablishmentOverviewDTO` in
  `@coralyn/contracts` + le due viste che lo consumano), fuori dal perimetro della slice di
  autorizzazione che aveva chiuso AUD-004.
  L'esecuzione ha **corretto due enunciati di questa voce**, e uno cambiava la gravità:
  1. «il link è nascosto dal menu ma l'URL è raggiungibile» era **falso**: il bottone col nome del
     lido in `SidebarNav.vue` fa `router.push('/establishment')` ed è **incondizionato**. Lo staff
     ci arrivava con un click.
  2. «più il gating della rotta `/establishment`» è stato **scartato con la ragione**: rilette tutte
     le card della pagina, ognuna delle superfici admin ha già `v-if="isAdmin"`, quindi tolte le
     email non resta nulla da nascondere — mentre il gate toglierebbe allo staff una pagina che usa
     legittimamente. Si è resa la rotta **innocua** invece di negarla.

  Il presidio è sul **payload**, non sul permesso, perché è il payload che può crescere: le chiavi
  esatte del DTO nel job veloce, «nessun `@` nella risposta» nelle e2e. Provato per mutazione:
  rimettere un campo con un'email fa **2 rossi**, uno per livello. Sul fronte le guardie sono due
  (query `enabled` admin-only **e** card `v-if`), e la mutazione ha mostrato che non sono ridondanti
  — togliendo solo la card le email **non** arrivavano a schermo, togliendo entrambe sì.

- <a id="d-066"></a>**D-066** — **`pnpm run verify` andava in OOM sotto pressione di memoria.**
  ✅ **CHIUSA il 2026-07-26** — [ADR-0061](decisions/0061-tetto-worker-runner-test.md): il tetto ai
  worker vive nelle configurazioni dei runner (`min(4, core/2)`, mai sotto 1), non nello script
  condiviso con la CI, che resta intatto.
  **La proposta di questa voce è stata misurata e scartata.** `--workspace-concurrency=1` da solo
  vale **−15 % di memoria per +33 % di tempo**, e soprattutto non tocca il caso che l'addendum qui
  sotto documenta: `ui-kit` da sola faceva **5.338 MB** di picco, più della RAM libera dello
  scenario — e su un pacchetto solo non c'è concorrenza fra pacchetti da limitare.
  Effetto misurato: gate completo **9.615–10.473 → 3.737–4.099 MB** (due corse per configurazione);
  `ui-kit` da sola **5.338 → 1.383 MB**; `apps/api` da sola **7.807 → 2.659 MB a parità di durata**
  (9 s), perché i 16 worker di `50%` erano sovradimensionati per 59 file. Costo dichiarato: il gate
  completo passa da **~36 a 61–88 s** — instabile fra corse, quindi non usarlo per misurare
  regressioni di durata.
  Presidio: [`test-workers.spec.ts`](../../packages/docs-lint/src/test-workers.spec.ts) — tre
  mutazioni, tre rossi (config senza tetto, costante non passata, ritorno alla percentuale).
  ⚠️ **Anche qui lo strumento si è rotto prima dell'oggetto**: il primo campionatore riportava 56 MB
  di picco e 493 MB di RAM libera contro i 14,5 GB misurati un minuto prima — l'operatore `-f` di
  PowerShell è locale-dipendente. Riscritto e validato su un caso a risposta nota prima di crederci.

  *Il testo storico della voce, per la ragione per cui era stata rimandata:*

  **`pnpm run verify` va in OOM quando la macchina è sotto pressione di memoria.**
  Trovata eseguendo la Fase G2, il 2026-07-25. `verify` chiama `pnpm -r test`, che lancia le suite
  dei **sette pacchetti in parallelo** senza alcun limite di concorrenza: `apps/api` (jest, già
  limitato a `maxWorkers: '50%'` **al proprio interno**) più quattro processi vitest, tutti insieme.
  Con **5,5 GB liberi su 33,5** l'esito è `FATAL ERROR: Zone Allocation failed - process out of
  memory` in `ui-kit` e `Error: spawn UNKNOWN` in `apps/api`; con **17,9 GB liberi** la stessa
  identica revisione esce **0**.
  ⚠️ **Non è la flake di P6-007, che è stata corretta**: quel fix limita i worker *dentro* jest, e
  regge (`apps/api` da sola passa sempre). Qui il moltiplicatore è la concorrenza **fra pacchetti**,
  che nessuno limita — la stessa radice **R5** dell'audit («i cancelli vanno nella configurazione»)
  applicata un livello più in alto.
  Misurato, non dedotto: il crash **si riproduce con la modifica di G2 messa da parte** (`git stash`
  → 5 righe `FATAL ERROR`), quindi non dipende dai test aggiunti; e le sette suite lanciate **una
  alla volta** sono tutte verdi.
  *Perché rimandata*: la correzione (`pnpm -r --workspace-concurrency=N test`, o separare `test` da
  `test:ci`) tocca lo **script condiviso su cui gira anche la CI**, e la scelta di `N` è un
  compromesso fra tempo di gate e memoria: è una decisione dell'utente, non un ritocco.
  *Trigger*: il primo verde/rosso non riproducibile che faccia dubitare del gate, o una macchina di
  sviluppo con meno RAM. *Impatto se ignorata*: **media** — non è un difetto di prodotto, ma è il
  gate che tutto il resto usa per dirsi verificato, e un gate che fallisce per ragioni ambientali
  insegna a ignorarne il rosso. In CI oggi non si manifesta (runner dedicato, un job per volta).

  ⚠️ **Addendum 2026-07-26 — la diagnosi sopra è incompleta, e la correzione proposta non basterebbe.**
  Misurato con **3,7–5,1 GB liberi** e lo stack Docker acceso (5 container), lanciando **una suite
  alla volta**, cioè con la concorrenza fra pacchetti già a 1:

  | Comando | Esito | Il vero numero |
  |---|---|---|
  | `pnpm --filter @coralyn/api test` | `FATAL ERROR: Zone Allocation failed` ×6 → **`Test Suites: 26 failed, 33 passed`** con **`Tests: 230 passed, 0 failed`** | 384 su 59 suite |
  | `pnpm --filter @coralyn/ui-kit test` | `Error: spawn UNKNOWN` (errno −4094) → `Test Files 21 passed (26)`, poi `23 passed (24)` **sulla stessa revisione** | 212 su **39** file |
  | `pnpm --filter @coralyn/web-staff test` | `Errors 24` → `Tests 203 passed` | 413 su **57** file |

  Con `--maxWorkers=2` tutte e tre escono **verdi ai numeri di baseline**. Tre conseguenze:

  1. **«le sette suite lanciate una alla volta sono tutte verdi» non è più vero**, e la riga qui
     sopra che lo dice va letta come vera *a 5,5 GB*, non in assoluto. La verifica valida sotto
     pressione è una alla volta **e con i worker limitati**.
  2. **`--workspace-concurrency=N` da solo non chiude questa voce.** Oggi il crash è arrivato con la
     concorrenza fra pacchetti già a 1: il moltiplicatore residuo è la concorrenza **dentro** ogni
     runner (jest è a `maxWorkers: '50%'`, che su molti core resta tanto; le vitest non hanno alcun
     limite). La decisione dell'utente riguarda quindi **due** assi, non uno.
  3. ⚠️ **Il modo in cui fallisce è peggio del fallimento.** Una suite che non parte contribuisce
     **zero** test al totale, quindi l'esito somiglia a «suite più piccola ma verde»: `Tests: 230
     passed, 0 failed` senza un solo rosso. Nel caso di `ui-kit` era sbagliato anche il
     **denominatore** — vitest dichiarava 26 e 24 file totali dove sono 39. **Il totale dei test va
     sempre letto insieme al conteggio delle suite/file, e confrontato con la baseline.**

- <a id="d-065"></a>**D-065** — **Package FE condiviso per il data-layer.**
  ✅ **CHIUSA il 2026-07-25** — [ADR-0058](decisions/0058-package-data-layer-condiviso.md),
  `packages/data-layer` (`@coralyn/data-layer`), 32 test propri.
  Approvata dall'utente lo stesso giorno; l'esecuzione ha **corretto due volte l'enunciato** di
  questa voce, e le correzioni valgono più della chiusura:
  1. «Cinque file identici in **due** copie» era riduttivo. A commenti rimossi: `toasts.ts`,
     `useQueryResource.ts` e `ToastHost.vue` (che non era nell'elenco) sono identici in **tre**
     copie, `web-customer` inclusa; `onApiError.ts` e `queryClient.ts` avevano codice identico e
     divergevano **solo nei commenti**.
  2. «Il nodo tecnico è che `ApiError` è definito per app» era **rovesciato**: la classe è
     testualmente identica in tutte e tre le app, e portarla nel package è ciò che *scioglie* il
     vincolo invece di subirlo. Nessuna iniezione di tipo è servita.
  3. «Non toccare `web-customer`» era **giusto ma troppo largo**: vero per `apiFetch` (il refresh
     single-flight di [ADR-0049](decisions/0049-auth-cliente-provisioned-tenant-pubblico.md) è
     rimasto fuori, e resta fuori), falso per i tre artefatti byte-identici, che con ADR-0049 non
     c'entrano nulla. Il vincolo come scritto avrebbe lasciato in piedi tre duplicati misurati.

  La duplicazione era **scoperta**, e lo si è provato per mutazione invece di dedurlo: togliere il
  `Bearer` da `apiFetch` di `web-platform` lasciava **29/29 verdi**, la stessa mutazione su
  `web-staff` faceva **2 rossi**. I test esistevano già, puntati su una copia sola.
  Fattorizzazione **per strati**, non per app — criterio: «ciò che non conosce né il router, né lo
  store, né la chiave di sessione dell'app». Il sistema toast è andato in `ui-kit`, dove
  `Toast.vue` viveva già. **Non fatta di proposito**: `authToken.ts` di `web-staff` e
  `web-platform` differisce per una sola stringa e resta duplicato (vedi ADR-0058, *Alternatives*).
